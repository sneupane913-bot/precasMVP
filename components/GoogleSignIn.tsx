/**
 * SUPERSEDED by components/FirebaseSignIn.tsx.
 *
 * Should be deleted; the iCloud-synced folder refuses programmatic deletion.
 * Listed in CLEANUP-AND-PUSH.command. Exports nothing so it cannot be imported
 * by accident and quietly become a second sign-in path.
 */
export {};
