# CONTINUE-HERE

**Read this first, every session, before touching anything. Do not start over. Do not re-plan from scratch.**
Append to the Work Log at the bottom before you finish. Keep this file accurate: it is the only thing that survives between AI sessions.

---

## 0a. HOW TO TREAT THE CLIENT (binding rule, do not break it)

**Never hand the client a command to run. Never say "run this on your Mac".** He is the client, not the operator. If something must happen on the Mac, **you do it yourself** with the computer-use tools: open the app, click, read the result, and fix any error you see. He should only ever be asked to approve an access dialog or make a genuine business decision.

Why: every time he was handed a terminal command it errored, and he had no way to fix it, so the loop repeated and burned sessions.

How to actually do it (this is proven and works):
- **Terminal is granted at tier "click": you can see it and click, but you cannot type into it.** So do not try to type commands there.
- Instead: **write a `.command` script with the Write tool, `chmod +x` it via bash, then use Finder (tier "full") to double-click it,** and read the output from a screenshot. Fix, rewrite the script, double-click again. That is the working loop.
- `request_access` for Terminal warns you once and tells you to retry in the same turn. Retry immediately in that same turn.
- Navigate Finder with `cmd+shift+G` then type the path (typing works in Finder).

## 0d. THE TEST PLAN IS THE GATE (binding rule, added 13 August 2026)

**`docs/TEST-PLAN.md` is the release gate. Read it before you touch anything.
Nothing goes to the client as finished until it has been walked from the top.**

It contains: every page and every control with what must happen and what must
never happen, the five actor lifecycles as flowcharts, the invariants, the
regression gate, the open defects, and the defects already fixed.

### What the client has actually asked for, and the standard he expects

He has said this more than once, and every time it was because a previous pass
covered what he named and stopped there:

> "I don't want you to just create this all thing based on my saying. I see that
> the last session you checked it only for the fixes which I had told you about.
> But I want every fix in this website, for every link, for every life cycle
> that there is. You should start from opening the application, start from
> checking each of the pages."

> "You're gonna go to the website. You're gonna see how many links are there,
> what are the things that can happen, what are the things that can go wrong,
> and what should we test for. Every single test, so that we ensure none of the
> bugs are there. When a student clicks on a certain link, the link has to be
> there. When a student does a certain action, the action has to be recorded."

**Treat what he names as examples, never as the scope.** When he describes one
scenario, he is showing you the KIND of thing to hunt, not the list. Cover the
whole class.

### Three things that must change about how testing is done here

1. **The automated suites are not enough, and this is proven.** All six suites
 were green on 13 August 2026, and within minutes the client found two real
 bugs in a browser (D-01 and D-02 in the test plan). Every suite drives the
 API and reads server rendered HTML. **Not one of them loads the JavaScript.**
 Every defect he has found in the last two rounds lives in client side state.

2. **So every pass includes a real browser pass** using the Claude in Chrome
 tools, walking Part A of the test plan screen by screen, clicking every
 control. He has offered to help with the real Google sign in when the flow
 needs it. Take him up on it.

3. **Never report "all green" from suites alone.** Say which suites passed and
 say plainly that the browser pass has or has not been done. He has been told
 "it is clean" and then found bugs himself, and that is the thing that has
 cost the most trust on this project.

### The bar for calling something done

- Clicked, not read. A route handler that looks correct proves nothing. The
 consultancy approval queue existed on the server for weeks with no button on
 the screen, and the code read perfectly the whole time.
- Zero findings means you did not look hard enough. Go to the wrong turns: the
 second tap, the back button, the expired session, the shared machine, the
 student who signs out and back in.
- A test is written before the fix, and watched failing first. A test that has
 never failed has proved nothing.

## 0b. RULES.md IS THE SOURCE OF TRUTH (binding rule, added 13 Aug 2026)

**`RULES.md` in the project root outranks every other document, including the
checklists and including the code.**

The client called this out and he was right. We built the product first and
wrote tests afterwards. A test written after the code can only confirm what was
built; it cannot tell you what *should* have been built. That is exactly why
every suite was green while a real student could not stay signed in, there was
no independent statement of correct behaviour for the code to be wrong *about*.

The order is now: **rules → code → QA checks code against rules.**

- Every rule has an ID (G-1, S-25, C-13, M-8 …). The coder builds to the ID.
 **Every test names the rule it proves.** A test that proves nothing in
 `RULES.md` is not a test, it is a habit.
- **If a rule is missing, stop and write it there first.** Never make the
 judgement call in code. Undefined behaviour is a gap, not a licence.
- If code and rules disagree, the rules win, or the rule is wrong and gets
 changed in `RULES.md` first, never silently in code.
- Statuses in that file are `[BUILT]`, `[BUILT+PROVEN]`, `[TODO]`, `[OPEN]`.
 **Nothing is `BUILT+PROVEN` until a passing test cites its rule ID.**
- Part 10 of `RULES.md` lists the rules with **no test at all**. That list is
 the honest gap. Never describe anything on it as working.

---

## 0c. THE CHECKLISTS ARE THE PLAN (binding rule)

**`CHECKLIST.md` in the project root is the index. It points at three lists** because three different people need three different things and mixing them is
how work gets missed. The client asked for this split on 12 August 2026.

| File | Who | What | How often |
|---|---|---|---|
| `CHECKLIST-DEV.md` | whoever is coding | everything still to be BUILT, in build order, with the score table | work top to bottom |
| `CHECKLIST-QA.md` | whoever is QA | everything to be CONFIRMED, a release gate, 8 gates including the per-button lifecycle | **from the top, EVERY release** |
| `CHECKLIST-MARKETING.md` | marketing analyst | claim honesty, pricing integrity, funnel, channels, competitor watch | claims before every public change, rest monthly |

**The difference that matters:** the developer list is ticked once and moves on.
The QA list is run again from the top every single time, forever, because its
whole job is catching the thing that used to work.

Rules:
- Work top to bottom through `CHECKLIST-DEV.md`. Do not jump around.
- Nothing is `[x]` until it has been built, typechecked, built, and **seen working**. `[~]` means partly done. If you could not run it, say so, never tick it.
- **Update the statuses and the score table every time you finish work** in the same commit.
- New scope goes into the correct section in order, never appended at the end.
- **Anything the client decides goes into `CHECKLIST.md` under "Decisions taken"** so it is not re-litigated by the next person.
- **Anything you got wrong and corrected goes under "Corrections on the record"** in the same file. A wrong number that is quietly fixed comes back.
- Before handing anything to the client as finished, run `CHECKLIST-QA.md` gates 1 to 3 at minimum.
- When the client asks "where are we", read him the score and the next few items, nothing more.

## 0b. HOW TO REPORT BACK TO THE CLIENT (binding rule)

**Give a short gist. Not a report.** He does not want to read a wall of text explaining everything you did.

**Every closing message is a GIST and nothing else.** It must be short and cover exactly three things, in plain language:
1. **What I did.**
2. **What I found** (anything broken, surprising, or worth knowing).
3. **What is next** (or what he must decide, if anything).

Target length: about 5 to 10 short lines total. No headings, no bullets-within-bullets, no walls of text.

Do not include: how you did it, which files you edited, what you learned along the way, section-by-section recaps, or any summary of your own process. All detail goes into the files (this one, `HANDOFF.md`, `docs/`), never into the chat. He has said repeatedly that long replies are confusing and he cannot follow them. If he wants detail he will ask.

## 0. THE MINDSET (read this before you write code)

The client has repeatedly hit one problem: an AI builds "the core MVP feature" and stops, leaving something that works but does not look or feel like a real product. **That is the failure mode to avoid.**

The standard is: **a deployable product a real student would trust and pay through.** That means the non-functional parts are not optional. Header, footer, legal pages, empty states, error states, loading states, real logos, consistent design, working buttons everywhere. If a page looks unfinished, it is not done, even if the logic works.

Rules that come from hard experience on this project:
- Never claim something works until you have run it. Typecheck, build, and render it. curl-only testing has missed real bugs here before.
- Never swallow an error code in a catch block.
- Never show a number for something we did not measure (a score for an answer we never heard is the product's cardinal sin).
- Use the approved design in `docs/design-reference/`, but never its placeholder content (it invented a "£29 unlimited" plan; our real offer is the NPR 449 / NPR 799 one time packs).
- Copy style: plain simple English for nervous students with weak English. No em dashes.

---

## 1. WHERE THE PROJECT IS (updated 2026-08-12)

Project path: `/Users/umanganiroula/Developer/Content Karkhana/precas-mvp`
(It was moved out of iCloud on 2026-08-12. iCloud used to deadlock `tsc` and corrupt git. That is fixed.)

**Verified working right now:** `tsc --noEmit` exits 0, `next build` succeeds, all public routes render 200 with correct content.

Git state: HEAD is `d9dd2aa`, **committed and pushed to GitHub** (`master -> main`). Working tree clean.

**Committing from the sandbox** (the mount cannot delete files, so stale `.git/*.lock` files block normal `git commit`): use plumbing and write the ref directly.
```bash
export GIT_AUTHOR_NAME="Umanga Niroula" GIT_AUTHOR_EMAIL="snit.education@gmail.com"
export GIT_COMMITTER_NAME="$GIT_AUTHOR_NAME" GIT_COMMITTER_EMAIL="$GIT_AUTHOR_EMAIL"
rm -f /tmp/pidx
GIT_INDEX_FILE=/tmp/pidx git read-tree HEAD
GIT_INDEX_FILE=/tmp/pidx git add -A
TREE=$(GIT_INDEX_FILE=/tmp/pidx git write-tree)
C=$(git commit-tree "$TREE" -p HEAD -m "your message")
printf '%s\n' "$C" > .git/refs/heads/master # bypasses the stale lock
```
(The Write tool refuses `.git` paths; use bash for that last line.)

**Pushing** must happen on the Mac (no GitHub credentials in the sandbox). Use `PUSH.command` in the parent folder and double-click it via Finder yourself (see section 0a). Two gotchas already solved and baked into that script: clear all four stale lock files, and set `http.version HTTP/1.1` plus `http.postBuffer 524288000`, otherwise a push of this size fails with `RPC failed; HTTP 400 curl 22`. The remote branch is `main`, the local branch is `master`, so the refspec is `master:main`.

---

## 2. HOW TO BUILD AND VERIFY (the workflow that works)

The cowork mount allows creating files but **not deleting** them, which breaks `next build` (it cleans `.next`). Use a build mirror in `/tmp`:

```bash
# one time PER SESSION (the /tmp mirror is empty in a fresh sandbox, and
# `next build` fails with "Could not find the Next.js package" without this)
mkdir -p /tmp/precas && /tmp/sync.sh && cd /tmp/precas && npm install

# sync source -> mirror (run after every edit)
cat > /tmp/sync.sh <<'EOF'
#!/bin/bash
SRC="<your session mount>/mnt/Content Karkhana/precas-mvp" # changes every session, check with pwd
DST=/tmp/precas
cd "$SRC" || exit 1
tar cf - --exclude=node_modules --exclude=.next --exclude=.git --exclude=docs/design-reference . 2>/dev/null | (cd $DST && tar xf -)
EOF
chmod +x /tmp/sync.sh

# typecheck (runs fine on the real folder)
cd "<project>" && ./node_modules/.bin/tsc --noEmit

# build + render test (MUST start server and query it in ONE bash call:
# background processes are killed between calls)
/tmp/sync.sh && cd /tmp/precas && npx next build
cd /tmp/precas && (npx next start -p 3000 > /tmp/next.log 2>&1 &) \
 && for i in $(seq 1 30); do sleep 2; node -e "require('http').get({host:'127.0.0.1',port:3000,path:'/'},r=>process.exit(0)).on('error',()=>process.exit(1))" 2>/dev/null && break; done \
 && node -e "const http=require('http');http.get({host:'127.0.0.1',port:3000,path:'/'},r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>console.log(r.statusCode,d.length))})"
```

Gotchas that cost hours before:
- `curl` in this sandbox goes through a proxy that returns fake 200s with empty bodies for localhost. **Use node http, not curl,** for local testing.
- The sandbox proxy blocks `fonts.googleapis.com`, so `next/font/google` cannot build here. Fonts are loaded with `<link>` tags in `app/layout.tsx` instead. Keep it that way: it also stops a Google Fonts outage from failing a Netlify deploy.
- The sandbox cannot reach `*.netlify.app` either. Live-site QA must go through the Chrome extension on the Mac.

---

## 3. DESIGN: the approved system

The client generated and approved a full design set in Google Stitch and it is saved in the repo at **`docs/design-reference/`** (8 screens, each `code.html` + `screen.png`, plus `academic_clarity/DESIGN.md` with the tokens). **Build every page to match these.**

System name "Academic Clarity": Deep Navy Ink `#0F172A`, Emerald `#10B981` accent, white and light slate `#F8FAFC` surfaces, **Noto Serif** headings, **Hanken Grotesk** body, 8px spacing base, small radii, minimal shadow, 48px tap targets, mobile first at 375px, max container 1140px.

Approved screens: landing_page, sign_in, universities_catalogue, results_report, consultancy_b2b_page, consultancy_admin_dashboard, super_admin_dashboard, owner_kill_switch.

**Use the layout, never the placeholder content.** Real offer: free 10 question trial, then one time packs **NPR 449 (6 mocks + 15 practice)** and **NPR 799 (12 mocks + 30 practice)** each mock **17 questions**. Starter and Pro stay hidden.

Full plan and per screen notes: `docs/DESIGN_DIRECTION.md`. The UX gap analysis: `docs/UX_AUDIT.md`.

---

## 4. WHAT IS DONE (verified, not claimed)

- **Firebase sign-in root cause found and documented.** The live `auth/api-key-not-valid` was a **corrupted value in the Netlify env var** `NEXT_PUBLIC_FIREBASE_API_KEY` (39 chars, right tail, wrong head, did not start `AIzaSy`). The code was correct. Fix: re-paste `AIzaSyAcabpaQyPunOK9qwjC0VhBBJUW0d0BN-Q` in Netlify (Production) and **Clear cache and deploy**. Dead theories: missing vars, Firefox tracking protection, undefined key. Only 3 Firebase vars are used by the code: API_KEY, PROJECT_ID, AUTH_DOMAIN.
- **`firebase` npm package was declared but not installed.** Fixed with `npm install`. This alone would have broken the build.
- `components/SiteHeader.tsx`: sticky header, logo links home, nav (Universities, How it works, Pricing), Sign in, solid Start free button.
- `components/SiteFooter.tsx`: brand block, Practise / Help / Legal columns, WhatsApp contact, honesty disclaimer, dynamic year copyright.
- Header and footer wired into `/`, `/pricing`, `/universities`.
- `components/TrustedBy.tsx`: self-moving university logo slider (client asked for this), pauses on hover, static under reduced motion.
- Fonts: Noto Serif + Hanken Grotesk via link tags, wired through `tailwind.config.ts` and `globals.css`.
- Legal pages created so footer links resolve: `/privacy`, `/terms`, `/refund` (working drafts, need Nepali legal review).
- **Consultancy wholesale pricing removed from public `/pricing`** (a student must never see the NPR 240 seat price). B2B lives on the unlisted `/consultancy`.
- Pricing CTA is now a solid high contrast button instead of a disabled looking box.
- `/start` rebuilt to the approved two column sign_in design: value proposition and university logos on the left, single Google button on the right, brand links home, Privacy and Terms links. Firebase logic untouched.
- `/universities` now renders the **real university SVG logos** with monogram fallback. Ravensbourne is correctly excluded from the public list until written logo permission.

---

## 5. WHAT IS NEXT (in this order)

**A. Finish the UI (match `docs/design-reference/`)**
1. Home `/`: align hero, three step cards and the sample QUESTION ANALYSIS card to `landing_page/screen.png`. **Pricing teaser is DONE** and is now the shared `components/PricingPacks.tsx`.
2. Results `/results/[sessionId]`: match `results_report`, keep "Not assessed" and never a fabricated score.
3. `/consultancy` B2B page: match `consultancy_b2b_page`, holds the seat and bundle pricing.
4. Back office to the same standard: `/admin`, `/super`, `/owner` per their screenshots. Calm dashboards, cards, clear tables. No transcript content to any admin.
5. Add a 404 page with a way home. Add PWA icons (`/icon-192.png`, `/icon-512.png`, apple touch icon) which still 404.

**B. Then the lifecycle (the client's main goal, target: finished this week)**
The lifecycle spec is `docs/LIFECYCLE_BUILD_SPEC.md` and it is authoritative. Routes that already exist: `/checkout`, `/consultancy`, `/api/payment`, `/api/me`, `/api/session/[id]/consent`, `/api/super`, `/api/admin`, `/api/platform`. Verify each end to end, then close the gaps:
- Student direct: sign in, 10 free questions, report, choice to pay or view report, QR payment with payer details and screenshot upload, approval pending state, allocation on approval, practice.
- Student via consultancy link `/c/[slug]`: same, attributed to that consultancy, approved by that admin (super admin can also approve, and the admin sees a count of those).
- Admin: seats bought, seats used, own students only, never other consultancies, never transcripts.
- Super admin: everything segregated by source, approvals with audit tally, enable and disable, referral leaderboard, attribution report, CSV export.
- Owner: maintenance switch with audit trail, fails closed, always re-enablable.
- Lifecycle screens do **not** need new Stitch mockups. Generalise Academic Clarity: device check, interview room, the gate after question 10, QR payment, approval pending, soft deny, and all empty, loading and error states.

**C. Then QA the whole thing** against `docs/LIFECYCLE_BUILD_SPEC.md` section 12 (acceptance matrix) and section 5 (fraud and money abuse tests).

---

## 6. LOCKED PRODUCT DECISIONS (do not relitigate, they are in HANDOFF.md)

- Light gate: **Sign in with Google** for the trial. Phone verification moves to the **payment** step and is sent over **WhatsApp** not SIM SMS, because SMS OTP delays were a real problem.
- Trial abuse: composite of Google account + device fingerprint + IP velocity. **Never hard block shared Wi-Fi** (a consultancy lab is 30 legitimate students on one router). Flagging is a **soft deny**: the student can still browse and still buy, only the free 10 is held, and super admin can override from a review queue. Never auto ban.
- Full mock is **17 questions** about 30 minutes. Trial is the first 10. Paying unlocks the remaining 7 of that sitting and grants the pack.
- Referrals replace promo codes: **+1 free mock only when the referred friend actually pays** fraud guarded (not same device, Wi-Fi, Google account or payment source), with a super admin configurable lifetime cap.
- Urgency must be **honest**: real per student deadlines and real named campaigns with server side end times. Evergreen or client generated countdowns are a dark pattern and a HIGH defect.
- Consultancy pricing is hidden from students, on the unlisted one word route **`/consultancy`**.
- Backend: **Supabase** for accounts, ledger, orders, approvals, seats (the single JSON blob store loses writes under concurrency).
- Pilot cost to load: about **$40**. Google sign-in is free to 50k monthly users. See `docs/PILOT_COSTS.md`.

---

## 7. KNOWN OPEN DEFECTS (from QA, details in HANDOFF.md)

QA-203 CRITICAL unlimited unverified trials (the money leak once STT is live), QA-202 no rate limiting anywhere, QA-201 ownerId echoed and reused, QA-204 Behaviour shows 0% on a silent clean attempt, QA-208 consent not recorded with version and timestamp, QA-209 `GET /api/platform` public, QA-210 answer endpoint 500 on empty body, QA-211 PWA icons 404, LIVE-004 no build SHA surface, LIVE-008 scroll not reset on stage change.

**Open data gap:** the large UK university list Codex reportedly saved as CSV is **not in the repo**. Only the 6 approved universities and their 6 SVG logos exist. Find that CSV to populate the full catalogue and the logo slider.

**Deferred:** align colours to merotestbooking.com colour theory later. It is a client rendered React app so its palette needs a rendered inspection. The Stitch palette stands for now.

---

## Work Log (append only, newest at bottom)

- **2026-08-11 (QA):** Created this file. Corrected the Firebase diagnosis. Full lifecycle commit `db63ad4` exists but was never QA verified.
- **2026-08-11 (QA, live testing):** Root cause of `auth/api-key-not-valid` found: corrupted Netlify env var value, not a code bug. Verified the real key is accepted by Google.
- **2026-08-11 (QA, UX audit):** Full UI and completeness audit of the live site written to `docs/UX_AUDIT.md`. Good bones, missing the entire storefront layer.
- **2026-08-11 (QA as coder):** Built SiteHeader, SiteFooter, legal pages, removed the consultancy price leak, made the pricing CTA solid. Could not verify (iCloud broke tsc and the mount blocked builds).
- **2026-08-11 (Stitch design port begun):** Client approved a Stitch design set; saved to `docs/design-reference/`. Ported fonts and added the TrustedBy slider.
- **2026-08-12 (folder moved out of iCloud, first real verification):** Reconnected the project at `~/Developer/Content Karkhana`. **Discovered `firebase` was declared but never installed** and fixed it with `npm install`. Established the /tmp build mirror workflow. **First verified green build in this project's history:** `tsc --noEmit` exit 0, `next build` succeeds, all 8 public routes render 200 with correct content (header, footer, legal links, real packs 449/799, no Rs500 leak, no NPR 240 leak, no invented £29 plan, 6 university SVGs in the slider). Switched fonts off `next/font/google` to link tags so a Google Fonts outage cannot fail a deploy. Rebuilt `/start` to the approved two column design and wired real university logos into `/universities`, both verified rendering. Commits are blocked only by stale `.git/*.lock` files that the sandbox cannot delete: worked around with git plumbing, see section 1.
- **2026-08-12 (committed and pushed, client no longer runs commands):** Client made clear he must never be handed terminal commands to run. Added section 0a as a binding rule. Committed as `d9dd2aa` by writing the ref directly (bypassing three stale lock files), then pushed it myself by writing `PUSH.command` and double-clicking it through Finder with computer-use. First push attempt failed with `RPC failed; HTTP 400 curl 22` because the 2 MB of design screenshots exceeded the HTTP/2 push path; fixed by forcing HTTP/1.1 and a large post buffer. **Push succeeded: `b727474..d9dd2aa master -> main`.** Netlify rebuilding. Next: verify the deployed site shows the new header, footer, logos and sign-in design, then continue the UI rebuild (home hero, results, consultancy, back office) and move into the lifecycle.
- **2026-08-12 (Netlify build failure fixed):** Deploy of `d9dd2aa` FAILED with "Exposed secrets detected: NEXT_PUBLIC_FIREBASE_API_KEY". Netlify's secret scanner fails a build when an env var value appears in build output. A Firebase Web API key is public by design (it identifies the project, authorises nothing, and must be in the client bundle for sign-in; access is controlled by Authorised Domains). Fixed in `netlify.toml` with `SECRETS_SCAN_OMIT_KEYS` listing only the `NEXT_PUBLIC_*` values. The real secrets (OWNER_ACCESS_KEY, SUPER_ADMIN_PASSCODE, SESSION_SECRET) are deliberately NOT listed so the scanner still guards them. Committed `5448aad` and pushed (`d9dd2aa..5448aad`). **If a future deploy fails on secret scanning for another NEXT_PUBLIC_ var, add it to that same list, never disable the scanner entirely.**
- **2026-08-12 (full live audit, client rejected the current UI):** Client inspected the live site and rejected the design as unprofessional and not matching the approved Stitch screens. Full defect list written to **`docs/UX_AUDIT_ROUND2.md`** read it before coding. Headlines: **(B1 CRITICAL)** `POST /api/session/create` works with NO auth and the home CTA points at `/universities`, so the sign-in gate is completely bypassed; nobody may reach universities/interview/results without signing in. **(B2 CRITICAL)** `bpp.svg` and `uel.svg` are white artwork (`fill:#fff`) so they are invisible on light panels; normalise the logo wall to one ink tone. **(B3)** live `/` and `/universities` do not match `docs/design-reference/` (missing the second hero CTA, the three-steps section, the QUESTION ANALYSIS sample card, Most applied, filter chips, duration/questions stat boxes). **(H1)** pricing needs higgsfield-style hierarchy: keep free, make **799 the large focal card** and 449 smaller. Plus dead ends: `/results/fake-id` 404 with no way home, `/interview/fake-id` returns 200 for a non-existent session, bare 404 page, no shell on `/consultancy` and `/checkout`, back office unstyled. Fix order is at the bottom of the audit file. Verified good: all 15 routes respond, every internal link resolves 200, legal pages fine, pricing content honest.
- **2026-08-12 (round 2 fixes coded and verified, commit `aaeb3e2`, PUSH PENDING):** Fixed B1, B2, B3, H1 and M1 from `docs/UX_AUDIT_ROUND2.md`. **Auth gate:** `POST /api/session/create` now requires `currentStudent()` and returns 401 `NOT_SIGNED_IN`; home CTA goes to `/start?next=/universities`; `/universities` checks `/api/me`, labels the button "Sign in to start" when signed out, routes to sign-in and returns to the chosen university, and recovers from a mid-session 401. **Logos:** every mark now renders via `[filter:brightness(0)]` at low opacity (bpp/uel are white artwork and were invisible); applied on the home strip, the sign-in panel and the university cards. **Home:** added the second hero CTA "See how it works", the "Three steps to interview readiness" section and the sample Question analysis card, per `docs/design-reference/landing_page`. **University cards:** logo chip, Duration and Questions stat boxes, Free first try badge. **Pricing:** NPR 799 is now the large dark focal card (bigger price, emerald badge and CTA, slight scale up), 449 smaller beside it, free card kept first. **404:** branded `app/not-found.tsx` with routes back to the catalogue and home. All verified locally: `tsc` exit 0, `next build` passes, create returns 401 without login, and every change confirmed present in rendered HTML. **NEXT ACTION: push `aaeb3e2`** by double-clicking `PUSH.command` via Finder (the Mac was locked when this was written), then re-verify the live site. **Still open from the audit:** H2 shell on `/consultancy` and `/checkout`, H4 `/results/*` dead end, H5 `/interview/fake-id` returning 200, M2 home FAQ section, and then the lifecycle work.
- **2026-08-12 (back office + consultancy link, commit `3170dc3`, pushed):** **`CHECKLIST.md` is now the master plan** (see rule 0c). Score moved 57/124 to 74/124. Rebuilt `/super` to the approved design AND rewired it from `/api/platform` to `/api/super` (it was blind to payment orders, flagged trials, attribution and referrals). Rebuilt `/admin` to its approved design and **fixed a real bug: the admin API read the old platform store, so every Google-signed-in student was invisible to their own consultancy**; it now reads `repo().listStudents({consultancyId})` and returns engagement plus entitlement only, never answer content. Added the owner switch history panel (H3) with an append-only `ownerAudit` in `PlatformSettings`. Built **`/c/[slug]`** the branded consultancy entry page, and made signup through it **bind the student to that consultancy server-side, only when the slug resolves to an approved consultancy** (binding is attribution, not entitlement, so a forged link buys nothing); unknown or unapproved slugs 404. Closed K8/QA-209: the public platform read returns only `maintenanceMode:false` when up, and the contact message only when down, which students need. Verified: tsc clean, build passes, all back office pages render, unknown slug 404s. **Next five are listed at the bottom of `CHECKLIST.md`** starting with the checkout screen and the money guarantees (E5 to E8).
- **2026-08-12 (session 3):** Client asked for the home pricing block to be replaced with the `/pricing` design, which he approved. Extracted both into `components/PricingPacks.tsx` (`<PricingPacks />` and `<PriceComparison />`) and pointed home and `/pricing` at it, so they render byte-identical cards and can never drift again (they did once: QA-205, home advertised a monthly plan we never sold). Home shows the packs plus a link to the comparison; the comparison table stays on `/pricing` only so home does not become a second pricing page. Verified by rendering both pages and diffing 12 content markers, all matching, hidden Starter/Pro not leaked.
 **Testing note worth keeping:** a naive `body.includes("NPR 449")` gives FALSE NEGATIVES, because React inserts `<!-- -->` between adjacent text nodes in SSR output. Strip comments and tags before asserting on rendered text. My first run reported 6 false failures because of this.
- **2026-08-12 (session 3, part 2):** Ran a real end-to-end verification instead of trusting the checklist. **Ten items were already built but were marked open** because nobody had tested them. Wrote `qa/lifecycle-check.js`, a 14-assertion regression suite covering the whole money journey, **all 14 passing**: sign-in, server-owned entitlement, authority-field injection ignored, ownerId not echoed (K6), stranger 404, empty body 400 (K9), consent recorded and stale versions refused (D12), silent attempt scores behaviour 100 not 0 (K7), server-owned price, hidden packs refused, duplicate wallet txn refused (E6), idempotent allocation (E8), and paying lifting the sitting 10 to 17 (D16/D17). Checklist went 74 to **84 done + 5 partial of 126**.
 **Two testing traps cost me two runs, both documented in `qa/README.md`:** (1) leave `.env.local` in the /tmp mirror and the dev sign-in token is correctly refused by the production guard, so everything downstream fails for the wrong reason, delete it from the MIRROR ONLY; (2) `session/create` sets the owner cookie, not `auth/firebase`, so a test must carry a cookie jar across the whole journey or four unrelated assertions 404.
 **Next, in order:** D13 to D15 the question-10 gate, E5 and E7 the checkout and approval-pending screens, K11 and K12 the two dead ends, F7/F9/F10 seat atomicity and tenant isolation, then B21 and B23.
- **2026-08-12 (session 3, part 3, the q10 gate, checkout, dead ends, and a real money bug):** Closed **D13, D14, D15, E5, E7 and K11** and found a defect nobody had reported.
 **The question-10 gate (D13 to D15).** `components/TrialGate.tsx` gives two real choices. "See my report" is offered as an equal option, not a grey escape hatch, and the report a free student gets is the *same* report a paying student gets for those ten answers, weakening it to force a sale would make us the thing this product exists to be the opposite of. No countdown on that screen: urgency before the student has seen any value is a dark pattern. `LockedNotice` covers D15 and always names what is locked and what unlocks it, because a bare disabled button reads as "broken", and a student who thinks the product is broken leaves.
 **Checkout (E5) and the pending screen (E7).** The QR comes from `PAY_QR_IMAGE_URL` (see `.env.example`) and the wallet number is *always* shown and copyable beside it, because most students scan from the same phone that is displaying the page, which cannot work. New endpoint `POST /api/payment/screenshot` stores the receipt in a `precas-receipts` Netlify blob store, guarded on ownership first, then type, then a 2 MB cap. **The upload is optional on purpose and a failed upload never blocks the payment** the control that stops a forwarded screenshot being reused is the unique wallet transaction id, not the image. The pending screen now shows the student their own amount, transaction number and an 8-character reference to quote back to us.
 **The dead ends.** New `components/RecoveryScreen.tsx` is now the single recovery screen for both `/results/{unknown}` (K11, closed, render-verified, correct 404 status) and `/interview/{unknown}` (K12, marked partial). Neither screen says *which* reason applies, deliberately: distinguishing "does not exist" from "is not yours" would confirm to a stranger that a guessed id is real, which is exactly the leak LIVE-002 closed. K12 stays partial and honest, its markup is proven through the results route, but the branch that selects it is client-side and **needs one click on the deployed site**. No browser can be installed in this sandbox (Playwright cannot get root), so I will not claim it renders.
 **The bug.** `consume()` in `lib/entitlement.ts` **had no callers**. Credits were granted on payment and never debited, so one NPR 449 pack bought unlimited mock interviews. Now called from the answer route, after the audio guard and before the paid STT call, and made idempotent per session, without that guard a 17-question mock would have burned 17 credits, which is the same bug in the opposite direction. Debiting on the first *answer* rather than at session creation is what makes the interview recovery screen's promise ("starting again does not use up a mock unless you record an answer") literally true.
 **Verification:** `tsc` clean, `next build` passes, `qa/lifecycle-check.js` now **20 of 20 passing** (added E5, E5-own, E5-type, E5-size, E5-ok and I-spend). Checklist **84 to 92 done + 6 partial of 126, about 73 percent**.
 **Two more testing traps, both cost a run.** (1) The suite must run against `next dev`, never `next start`: `next start` sets NODE_ENV=production, which correctly disables the `dev:` sign-in token, and every assertion then fails for the wrong reason. (2) The suite used `superKey: 'sup-x'`, which only worked because it was reading the client's real `.env.local`; it now uses the documented dev fallback `super-dev` so it does not depend on a secret. Also: **background servers do not survive between bash calls** start the server and run the suite in the *same* call, and never `rsync --delete-excluded` into the mirror or it deletes `node_modules`.
 **Next five are at the bottom of `CHECKLIST.md`,** starting with F7/F9/F10 seat atomicity and tenant isolation.
 **Pushed:** `aa6e26a..ef38ded master -> main` at 11:40, Netlify rebuilding. **Still to confirm on the deployed site: K12** open `/interview/some-made-up-id` and check the recovery screen appears rather than a spinner.
- **2026-08-12 (session 3, part 4, seats and tenant isolation, F7/F9/F10 closed):** Found the **same class of bug as `consume()`, again**: `allocateSeat()` was correctly written with per-index claim keys and **had no callers**. A consultancy could buy a hundred seats, sign up two hundred students, and the dashboard would show nothing used. Added `grantSeat()` in `lib/entitlement.ts` and wired it into the Firebase signup route, after the student row is created so a seat is never claimed for an account that failed to create.
 **A student is never turned away when the seats run out.** They keep their free trial and can buy a pack like anybody else; being the fifty-first student through a fifty-seat link is not their fault.
 **`SEAT_GRANT` is an OPEN DECISION and the client must confirm it.** Nobody had ever said what a seat is worth. Rather than invent a number I derived one from the client's own figures: every bundle costs us 118 NPR per seat, the Prep pack costs 241 for 6 mocks and 15 practice, so a seat is a shade under half a Prep pack, **3 mocks and 8 practice**. It is written down at the bottom of `CHECKLIST.md` under "Open decision needing the client". If a seat should be worth more, raise `SEAT_GRANT` **and** the bundle prices together, never one without the other.
 **Also fixed a quieter bug:** the admin dashboard counted revoked seats as used (`seats.length`), while `allocateSeat` has always filtered on `revokedAt`. Two places disagreeing about how many seats are left is how a consultancy gets told it is full when it is not.
 **New suite `qa/tenant-check.js`, 8 of 8 passing:** 6 simultaneous signups against 3 seats produce exactly 3 seated (balances `[4,4,4,1,1,1]`), all 6 accounts still created, injected `consultancyId`/`tenant`/`slugOverride` fields are stripped by the schema so A sees only A, A's passcode against B's slug 403s, a suspended consultancy reads nothing, and a direct student is invisible to every admin. **Honest limit: this proves the claim-key algorithm inside one process.** The distributed guarantee still rests on Netlify Blobs' write-if-absent and has not been tested against the real store.
 Each student in that suite signs in from a different `x-forwarded-for`. That is not dodging the rate limiter, five sign-ins per IP per five minutes is the control that makes passcode brute force impractical, and loosening it for a test would be testing a product we do not ship.
 **Verification:** `tsc` clean, `next build` passes, lifecycle 20/20, tenant 8/8. Checklist **95 done + 6 partial of 126, about 75 percent**.
 **Pushed:** `ef38ded..9d28c5c master -> main` at 11:49, Netlify rebuilding.
- **2026-08-12 (session 3, part 5, client decisions taken, E9 and E10 closed):**
 **I made an arithmetic error in part 4 and corrected it here.** I read `costNpr: 241` as the Prep pack's cost; it is Pro's. Prep costs 59 and Serious costs 118. The bundle files had always costed a seat at exactly **118 per seat** (2360/20, 5900/50, 11800/100), and 118 is precisely the Serious pack's cost, so the original intent was always that **a seat is a full Serious pack**. My "3 mocks" figure was built on the bad reading. Corrected, and `SEAT_GRANT` is now **derived from the Serious plan** rather than typed out, so it cannot drift again.
 **Client decisions, all recorded at the bottom of `CHECKLIST.md`:** (1) a seat is the Serious pack, 12 mocks and 30 practice, the same product a paying student gets, a consultancy student must never receive a lesser product than someone off the street; (2) bundles are **20 and 30 seats at NPR 300 each** (6,000 and 9,000), the 50 and 100 tiers dropped; (3) **consultancy admins may approve their own students' payments**; (4) the full button-by-button audit waits until the lifecycle is finished.
 **On (2), the client first asked for seats to be only "a bit" cheaper than the 799 retail.** I showed him the arithmetic instead of just doing it: at NPR 699 a seat he earns more per bundle but the consultancy has no room to resell under our own retail price, so nobody would buy. He chose NPR 300, which is what his cost sheet had always assumed.
 **On (3) I flagged the risk and he accepted it.** The money lands in OUR wallet, so a consultancy approving a payment is asserting something they cannot see. The mitigation is a paper trail rather than a block: `CONSULTANCY_APPROVAL_NOTE` is stamped into every such approval, and the order stays visible to the super admin.
 **Built:** `lib/payments.ts` now owns approve and reject, and BOTH `/api/super` and `/api/admin` call it. The moment two routes can release credits, one shared function is the only safe shape. The admin route checks `order.consultancyId === c.id` and returns the same 404 for "no such order" as for "not yours", so order ids cannot be probed across tenants. E10: when anyone other than the consultancy approves one of their students, that consultancy gets a notification, because a dashboard whose numbers move silently is a dashboard nobody trusts.
 **`qa/tenant-check.js` now 12 of 12** (added E9-cross, E9, E9-once, E10). Lifecycle still 20/20. Build passes, `/consultancy` renders the two new bundles and no longer shows 50 or 100.
 **A test-hygiene trap worth keeping:** the suite reused one IP for six super-admin calls, tripped the five-per-window auth limiter on the sixth, and an unrelated assertion then failed. Every super-admin call now uses its own `10.9.9.x`. **When a QA assertion fails, check the rate limiter before believing the product is broken.**
 Checklist **97 done + 6 partial of 126, about 77 percent**.
- **2026-08-12 (session 3, part 6, the checklists split into three):** Client asked for the one master list to become three, one per role, and for this handoff file to point at all of them so another AI can pick up cleanly.
 **`CHECKLIST.md` is now a short index** holding the score, the client's settled decisions, and my corrections on the record. It points at:
 **`CHECKLIST-DEV.md`** the old master list, everything still to be BUILT. Ticked once, stays ticked.
 **`CHECKLIST-QA.md`** NEW, and the one the client specifically asked for. A release gate run from the top every single time, in 8 gates: (0) the four traps that have already produced false results here, no `.env.local` in the mirror, `next dev` not `next start`, the 5-per-IP auth limiter, and stripping React's `<!-- -->` before asserting on HTML; (1) automated, must be 20/20 and 12/12; (2) every route answers and offers a way onward; (3) **the lifecycle of every button** 46 named controls, each checked in four states: normal, working, failure, recovery, because *a control that silently does nothing is the worst defect class in this product*; (4) the twelve money guarantees; (5) roles, privacy and isolation; (6) whether what is on screen is actually true; (7) a real phone on mobile data; (8) after the deploy, including that a failed Netlify build leaves the OLD site up looking fine.
 **`CHECKLIST-MARKETING.md`** NEW. Leads with claim verification because this product has already had to retract two claims ("60% cheaper for the same thing" compared different pack sizes; "no other platform lets you try free" was untrue). Then pricing integrity (the NPR 300 wholesale price must never reach a student-facing page), the five funnel numbers worth measuring, channels, competitor watch, and a short list of things marketing must NOT do, no fake countdowns, no pressure before the student has seen their report, no claims about visa outcomes, and never touching transcripts for marketing.
 **Rule 0c in this file was rewritten** to cover all three, and now also requires that client decisions go under "Decisions taken" and my own corrections go under "Corrections on the record" in `CHECKLIST.md`.
 **Pushed:** `482e020..8b563a5 master -> main` at 12:28.
- **2026-08-13 (session 4, the walks, eight bugs, and the test plan):**
 Built `qa/walk-check.js`, which is different in kind from the other five suites and is now the important one. The others prove individual guarantees in isolation. This one walks each ACTOR from their first click to their last, in the order they really click, with the wrong turns included: the second tap on bad wifi, the abandoned QR page, the sign out and back in, the second Gmail on the same laptop, the shared consultancy machine. **The bugs live between the steps, not inside them** and a list of endpoints cannot find them.
 **Eight real defects found and fixed.** Three of them were critical.
 (1) **No way to sign out anywhere in the product.** Not a missing nicety: this product runs in consultancy labs on shared machines with a ninety day cookie, so the next student to sit down was already signed in as the last one, could read their report and spend the credits they paid for. Added `components/HeaderSession.tsx`, `/signout`, and `POST /api/signout`. The page is a plain form so it works when the JavaScript never loads, which is exactly the machine where it matters.
 (2) **One student could have two payments waiting for approval at once.** The client predicted this one almost word for word. He pays once, opens the checkout again, types a slightly different number, and two requests sit in the queue for one payment. Whoever approves sees two, believes two, grants two packs. `create` now refuses while one is submitted and says plainly that his payment is already being checked.
 (3) **A consultancy could not see the payments only they were allowed to approve.** `/api/admin` fetched the orders, used them to count revenue, and never sent them to the browser; the portal page had no queue and no buttons. So the client's own rule, that a student from an admin link is approved by that admin, was unreachable. The server code read perfectly the whole time. **This is why nothing is passed by reading code.**
 Also fixed: a refused student got red text with nothing to click (refusals now carry an action the server chooses, rendered as a button); every checkout visit wrote a new order without limit; tapping "I have paid" twice on bad wifi showed red to someone who had really paid (submit is now idempotent for the same number); we told a consultancy when we approved their student and said nothing when we rejected; and a verified payment could be flipped to rejected while the credits stayed granted, because `/api/super` wrote the order itself instead of calling the shared `rejectPayment`. Removed `checkCredits()`, a placeholder in the answer route that always returned allowed and read like a control.
 **Result: walk 78/78, adversarial 18/18, lifecycle 20/20, journey 26/26, fraud 19/19, tenant 12/12, `tsc` clean.**
 **Then the client found two more bugs in a browser, within minutes, with every suite green.** `/start` never asks whether you are already signed in, so a signed in student who taps "My practice" is shown the Google chooser again; and the catalogue keeps saying "Sign in to start" after you have signed in. **Both are client side state, and not one of the six suites loads any JavaScript.** That is the real lesson of this session and it is written up as D-04.
 **New: `docs/TEST-PLAN.md`, and it is now the release gate.** Every page with every control and what must never happen, the five actor lifecycles as flowcharts, the invariants, the regression gate, the open defects, and the ones already fixed. **New rule 0d in this file** records the standard the client has set, in his own words, and requires a real browser pass with the Claude in Chrome tools on every release. Do not report "all green" from the suites alone again.
 **Open and NOT fixed, deliberately, because the client asked for documentation rather than more code:** D-01 `/start` ignores an existing session, D-02 stale signed in state on the catalogue, D-03 no visible confirmation of who is signed in, D-04 no browser level testing at all. They are the first work of the next session.
 **Two stale git lock files were left behind: `.git/HEAD.lock` and `.git/index.lock`, both zero bytes.** The sandbox mount cannot unlink them ("Operation not permitted"), and they will block the next ordinary git command. This has happened before and the fix is known: either have the client's Mac remove them (they are harmless empty files), or bypass them entirely with the git plumbing route already proven here, `write-tree` then `commit-tree` then writing the hash straight into `.git/refs/heads/master` with `printf`. Do not spend a session on this again.
 **Committed as `ac640f7` on the local repo. NOT pushed** because the client asked for documentation rather than more deployment this round. The next session should push it with the usual PUSH.command once D-01 to D-03 are also fixed, so one deploy carries the whole set.

- **2026-08-13 (PILOT HARDENING, client found real bugs the suites could not see):** The client opened the live site and hit a sign-in loop, no signed-in identity, no sign-out, and `/practice` bouncing to Google over and over. **The checklist said 120/126 done. It was not lying about the code; it was lying about what "done" proves.** Every suite here is API-level: it posts JSON and reads JSON. A redirect loop, a missing header, a dead link, none of that is visible to any test in this repo. That gap is the lesson: **green suites are not evidence a human can use the site.**
 **Root cause of three of the four: commit `ac640f7` was never pushed.** `HeaderSession`, `/signout`, `/account` and the header rework all existed locally while the live site ran `a1a919d`. Work was marked complete and never deployed. **Check `git log origin/main..HEAD` before telling the client anything is live.**
 **PILOT-01, the worst bug in the codebase.** `BlobRepo.get()` ended in `catch { return null }`, which made a BROKEN STORE indistinguishable from A STUDENT WHO DOES NOT EXIST. A student signs in with Google, the next request cannot read their record, `currentStudent()` returns null, every API answers 401, the page bounces to `/start`, they sign in again, forever, with nothing logged and nothing looking broken. `list()` had the same disease and would have shown a consultancy "0 students" instead of an outage. Both now throw `StoreUnavailableError`, and `lib/api-errors.ts` turns that into a **503 with an honest message** never a 401. This matters because every client page only bounces to sign-in on a 401, so returning 503 is what breaks the loop.
 **PILOT-02.** `/start` never asked whether you were already signed in, which is what turned one failure into an endless loop. It now checks `/api/me` first and sends a signed-in student straight on.
 **PILOT-03, hardening.** `claim()` decided "did I win?" by comparing the value it wrote against the value stored. Callers passed things like `{orderId, at: ISO}`, so two writers in the SAME MILLISECOND wrote byte-identical values and both believed they had claimed it. This is the primitive behind every uniqueness guarantee in the product, so its failure mode is money. It now uses an uncollidable token.
 **New `qa/pilot-check.js`, 29 case studies, all passing.** Named stories rather than unit tests, built from the client's own list: finish ten then log out and back in (no refill), exhausted student blocked, abandoned checkout grants nothing, returning three days later still works, **triple-tapped submit produces exactly ONE item in the approval queue** a forwarded receipt refused, admin A cannot approve admin B's student, super admin covering for a sleeping admin and the admin being told, both approvers clicking at once paying once, an ad-sourced student invisible to every admin, and no admin able to read a transcript.
 **Three of my own tests were wrong, not the product.** (1) CS-05 asserted a second Google account on one device should be stopped, the threshold is deliberately 4, because a consultancy lab shares machines and punishing the second student there would be indefensible. (2) CS-08 asserted only one of three repeat submits should succeed, answering all three calmly is deliberate, because a student shown a red error after sending real money pays twice or assumes fraud; what must not duplicate is the QUEUE ITEM, and it does not. (3) Lifecycle E6 tried to create a second order for one student, which is now correctly refused with 409, so it never reached the duplicate-txn guard; it now tests the real threat, a screenshot forwarded to a friend.
 **Harness lessons, both cost a run.** Next refuses a second `next dev` for the same project and silently reuses the first, so two suites tested nothing and reported "code 0". Kill the port between suites. And `fraud-check.js` shared one IP across more than ten payment calls, hitting the 10-per-hour limiter; it now varies `x-forwarded-for`, as `tenant-check.js` already did.
 **All seven suites green:** lifecycle 20/20, tenant 12/12, fraud 19/19, journey 26/26, walk 78 steps 0 bugs, adversarial 18/18, pilot 29/29.
 **STILL NOT PROVEN, and must not be claimed:** anything requiring a real browser, the Google popup and its redirect fallback, camera and microphone, the 360px phone pass, and the PWA install. No browser can be installed in this sandbox (Playwright cannot get root). These need a human with a phone.

- **2026-08-13 (THE RESET, rules first, and a new host):** The client stopped the work and named the real failure himself: *"I did not make a strategic plan. I did not make a skeleton of where everything needs to go. Once the rules were embedded, none of these bugs would have come."* He is right, and it is the single most important thing in this file.
 **What actually went wrong, stated plainly:** we wrote code, then wrote tests that described the code we had already written. Tests written after the fact can only confirm what you built. They cannot tell you what you *should* have built. That is why 124 assertions were green while a student could not stay signed in, nothing existed for the code to be wrong *about*. **Rules → code → QA. In that order, from now on.**
 **Created `RULES.md`** and made it outrank everything else (new binding rule 0b above). Five actors, ~120 numbered rules, each marked BUILT / BUILT+PROVEN / TODO / OPEN, and **Part 10 lists every rule with no test at all** so the gap is impossible to hide. The gap list is long and includes all of the AI, the whole student dashboard, and everything needing a real browser or phone.
 **We had forgotten an actor.** The student had no home of their own, no dashboard, no history, no sense of what they had bought. Rules S-38 to S-44 now cover it, all TODO. This is why "My practice" felt broken: there was nothing behind it worth arriving at.
 **HOSTING DECIDED.** Moving off Netlify to **Himalayan Host, "20X Faster Local Cloud with Free .COM", NPR 3,500/year** local Kathmandu datacentre, DirectAdmin, Node.js supported. Coupon `HH2026` for 10% off. Reasoning: a .com alone costs NPR 1,500–2,000/yr, so the hosting is effectively ~NPR 1,500, and local hosting matters for students on Nepali mobile data. `merotestbooking.com` is abandoned, a fresh domain.
 **TWO THINGS TO SETTLE BEFORE DEPLOYING THERE (OPEN-1, OPEN-4 in RULES.md):** (1) their pages say "Node.js supported", which normally means the CloudLinux/Passenger selector, that runs simple Express apps reliably but **Next.js 16 App Router is heavier and I cannot test their environment from here.** Open a ticket asking exactly: *"Can I run a Next.js 16 application on a persistent Node.js process with `next start`?"* If no, the NPR 12,000/yr Europe VPS is the honest answer. (2) **Netlify Blobs does not exist there.** Their shared plans give MySQL, not Postgres. Point at Supabase's free cloud tier (the schema is already written in `supabase/schema.sql`) rather than writing a MySQL layer.
 **PRICING CHANGED, NOT YET APPLIED (M-8 to M-12):** NPR 449 = **3 mocks + 15 practice**; NPR 799 = **10 mocks + 20 practice**. A consultancy seat follows the 799 pack. **Flagged for the client:** at 3 mocks, NPR 449 is NPR 150 per mock against the competitor's NPR 143–160, it is no longer clearly cheaper, so the comparison table on `/pricing` must be re-checked before this goes live or it becomes a false claim.
 **Still true and still unpushed:** commits `1efbb13` and earlier are waiting; the Mac was locked. Until they are pushed the live site still has the silent sign-in loop (PILOT-01).
 **HOSTING RE-DECIDED, same day.** Himalayan Host support was asked "can I run a Next.js 16 application on a persistent Node.js process with `next start`?" and replied with a link to the **VPS** page, not the shared hosting page. That is not an answer, but the choice of link reads as "you need a VPS for that", which fits: shared-plan "Node.js support" on DirectAdmin is the CloudLinux/Passenger selector, built for small apps and usually unable to keep a Next.js server resident. A VPS certainly works (root access; their own blurb mentions running n8n), but the cost jumps from NPR 3,500/yr shared to **NPR 12,000/yr Europe or NPR 42,000/yr KTM** and building one blind is most of a day out of three.
 **Decision: stay on Netlify for the pilot and migrate afterwards.** Free, already working, and a new domain points at it in about thirty minutes. Local hosting is a speed improvement, not a works-versus-does-not-work improvement, and speed is not what is short right now. Storage stays Supabase free tier (OPEN-4 settled).
 **If someone revisits this:** the precise question to put to the host is whether the shared plan can run a long-running Node 20+ process listening on a port behind their web server, staying resident between requests, and if not whether KTM Small is the cheapest plan that can.

- **2026-08-13 (TESTED introduced, and 11 TODOs built):** Client added a fourth rung to the ladder. A rule is now `TODO → BUILT → BUILT+PROVEN → BUILT+PROVEN+TESTED`, and **TESTED is granted live, in Chrome, with the client watching, never by me alone.** The legend at the top of `RULES.md` explains why: every suite was green while a student could not stay signed in, because API tests send JSON and read JSON and cannot see a redirect loop, a silent button or a stuck spinner.
 **Built this session, 11 rules TODO → BUILT:**
 **V-9, and it explains the iPhone 6s.** The client saw *no sign-in button at all*. Cause was structural: `/start` renders a grey placeholder while `loading` is true, and `loading` was cleared only after two awaited fetches both resolved, one stalled request on an old device meant the placeholder stayed forever. Now every fetch has a 6s timeout and `loading` clears in a `finally`. Also: iOS and in-app browsers (Facebook, Instagram, TikTok) now **skip the popup entirely** and go straight to redirect, because trying the popup there produces the worst outcome, a button that looks like it did nothing. Any unexpected popup error also falls back to redirect rather than dead-ending. And `FirebaseSignIn` can no longer render an invisible placeholder.
 **G-10 / E-6:** `components/ActionButton.tsx`. Working / failed / done are impossible to forget, the button locks while in flight (the cheapest double-payment protection there is), and errors attach to the button that caused them rather than floating at the top of a page a phone user will not see.
 **S-41, S-42, S-44:** the dashboard now shows progress and one thing to work on. **Trend is null until TWO scored sittings** one point is a dot, not a direction. The weakest sub-score **skips nulls** because a skill we could not judge is a gap in our measurement, not a weakness in the student; naming it would be the same sin as scoring silence.
 **S-37:** the report names one concrete action, directly under the verdict, because a student reading a low score is looking for what to DO and will not scroll past four sub-scores. Shared with the dashboard through `lib/advice.ts` so the two can never give different advice about the same weakness.
 **S-28:** a dropped connection now **preserves the recording** and offers "send the same recording again". Before, a failed upload told the student to re-record an answer they had already given, on a question with three attempts, because OUR request failed. On Nepali mobile data a dropped connection is the normal case, not the exception.
 **Verified:** tsc clean, build passes, and all seven suites still green, lifecycle 20/20, tenant 12/12, pilot 29/29, fraud 19/19, journey 26/26, walk 78 steps, adversarial 18/18.
 **5 TODOs remain, and 3 are blocked on the client:** Q-1 and Q-2 (researching what UK universities and UKVI credibility interviews actually ask, real content work, I can do it next), and Q-5, Q-8, Q-9 (Groq speech-to-text, the Nepali-accent benchmark, and AI privacy) which cannot start until the **Groq and Gemini API keys** exist. Both are empty in `.env.local`.
 **Next: the TESTED session.** Chrome, together, rule by rule.

- **2026-08-13 (Part 12: the 13 August model, written then surveyed then built):** Client rewrote the consultancy model and added a large amount of scope. Written into `RULES.md` **Part 12 as 47 numbered rules before any code** per rule 0b.
 **The hardest item disappeared on a clarification.** "Admin gives the student an id and password" would have meant a second identity system, storage, hashing, reset, lockout, a day of work. The client clarified: **sign-in is Google, always. The admin issues a LINK, not credentials.** One identity system, nothing to store, nothing to leak. `N-2`.
 **`C-12` is retired.** An admin never approves a student payment, because a consultancy student never pays us. Admins pay us for seats; how they collect from their own students, cash, their own link, or free, is their business and never touches our system.
 **`qa/model-check.js` was written BEFORE the code and run as a survey.** First run: 9 passed, 6 failed. Those six failures were the work list, and this is the first time on this project that the tests told us what to build rather than confirming what we had built.
 **What the survey found, and it was worth doing:**
 **N-26 was the serious one.** `buildQuestionPlan` was fully deterministic, **every student on the platform received the same ten questions in the same order.** Ten students in one lab compare notes, memorise the set, and walk into a credibility interview with rehearsed answers, which is precisely what that interview is designed to catch. We would have been coaching students into failing. Now **stratified random**: an identity question always opens, then one question per category in turn so the trial still diagnoses, with WHICH question random. Pure shuffling was rejected, it would sometimes hand a student six finance questions and tell them nothing about the rest. The plan is still stored on the session, so a resume stays deterministic (Q-4, N-45).
 **N-41/N-42, the kill switch had a hole.** `/api/me`, `/api/account`, `/api/super` and `/api/admin` never checked `platformDown`. A dark site with a working till is not a kill switch, and a super admin still able to move money while students are locked out is the exact workaround the switch exists to prevent. All now 503. Deliberately excluded: `/api/platform` (the owner needs it to turn the platform back on) and `/api/signout` (trapping a signed-in student on a shared lab machine would be worse than the outage).
 **N-40 was a real dead end.** Typing your own university and being told "we do not have that" with no button is the worst screen in the funnel. It now offers the general UK paper, because a credibility interview asks the same themes wherever you apply, the student loses almost nothing by practising it, and everything if we send them away.
 **N-44 to N-47 mostly already worked** and the survey proved it rather than assuming: answer 2 of 17, close the tab, come back at question 3 with the same paper and one credit spent. Only the dashboard's `resumeHref` was missing from the API contract.
 **Two of the six "failures" were my tests, not the product** the owner key is `owner-dev` not `own-dev`, and N-40's empty-search branch is not in the first paint so it needed a source-level check.
 **Suites: model 14/15, rules 59/59, lifecycle 20/20, pilot 29/29.**
 **N-29 is the one genuine failure left and it is deliberate: the question bank cites no sources.** Until that research exists, **no page may claim these are the questions universities ask.** That is the next piece of real work, and the client named it as the heart of the product.

- **2026-08-13 (the AI contract, fixed before the key):** Client's goal for today was that **everything except connecting the AI is finished** so that tomorrow is plumbing. Built `lib/ai/contract.ts` and Part 13 of `RULES.md` (AI-1 to AI-9, 8 of 9 BUILT+PROVEN).
 **Every input and output is now fixed before a provider is chosen.** What goes in: question, category, intent, transcript, duration, university, level, and **what the student said earlier in the same sitting**. What comes back: PEE + wrap-up, one Nepali line, three nullable sub-scores, and a **contradiction** field.
 **`AI-2` is the one I would defend hardest.** No name, email, phone, consultancy or payment history is in the input shape at all. A grader does not need to know who somebody is to say whether they named their course, and these transcripts contain family income and visa refusals.
 **`AI-4` contradiction detection is the highest-value output.** A student whose father is a farmer in question 3 and a businessman in question 11 has a problem no single-answer grader can see, and an officer certainly will. That is why `previousTranscripts` is in the input.
 **`looksGeneric()` enforces N-30 at runtime, on every response, not as a spot check.** A model under load drifts towards safe generic sentences because they are the highest-probability output, and that drift is silent. The guard requires the `evidence` field to actually overlap the transcript, and rejects a list of stock phrases ("give more detail", "keep practising"). On failure the prose is dropped, the score and recording are kept, and the student is told plainly: **a missing paragraph is honest, a generic one is a lie that costs them the interview.**
 **`AI-6`: accent and grammar are never penalised.** Only vagueness, contradiction, numbers that do not add up, and answers that sound recited. **`AI-7`:** the transcriber gets a language hint and a Nepali vocabulary hint, so it does not "correct" a speaker into words they never said and then quote those words back at them as evidence.
 **When the key arrives, only `stt.ts` and `evaluate.ts` change** (AI-9). G-1, N-30, Q-10 and G-8 must all still hold afterwards.
 **A third test of mine matched a code comment instead of code** the same mistake as S-16, this time on AI-2. The file explains in prose what is deliberately NOT sent; the assertion now parses the interface body. **Worth internalising: when asserting on source, parse the construct, never grep the file.**
 **Suites: model 22/23, rules 59/59, lifecycle 20/20, pilot 29/29.**
 **STILL OPEN, 30 of the Part 12 rules.** Largest remaining blocks: the seat model with variable mock counts (N-1, N-3, N-5, N-6), QR and WhatsApp deep links (N-9 to N-16), the abuse queue and device soft-block (N-18 to N-20), super-admin views (N-21 to N-24), question authoring (N-25, N-28), and practice (N-34, N-36, N-37).
 **N-29 remains the one deliberate failure: the question bank cites no sources.** Until that research exists no page may claim these are the questions universities ask, and the client has called the questions the heart of the product.

- **2026-08-13 (pricing applied, M-8, M-9, M-10, M-12):** Client confirmed. **NPR 449 = 3 mocks + 15 practice** (costs us ~30). **NPR 799 = 10 mocks + 20 practice** (costs us ~98). A consultancy seat follows the 799 pack and is **derived from it in code** so it can never drift.
 **M-12 mattered more than the numbers.** The pricing page compared **price per mock** and at 449 for 3 mocks we are **NPR 150 a mock against the competitor's 143–160** we are no longer cheaper on the one number the page was comparing. Leaving that table up would have turned a true claim into a false one (G-9). The client's own argument is stronger and is what the page now says: **our entry pack is NPR 449 against their NPR 799, and we let a student try ten real questions free while they do not.** The per-mock line is gone from the pack cards too, because printing NPR 150 invites exactly the comparison we lose.
 **The pricing change broke four suites and every break was a stale expectation, not a bug** E8 expected 6 mocks, CS-16 expected a 13-mock seat, E9 expected 6. All corrected to 3 / 11 / 3.
 **My own G-9 guard caught me twice** which is the best evidence it works: the new comparison table used the word "cheapest" in a column header and in its intro sentence, and the guard refused both.
 **And for the FOURTH time a test of mine matched a code COMMENT instead of code** M-12 grepped for "per mock" and found the comment explaining why the per-mock rate is not shown. Comments are now stripped before that assertion. **The lesson is now unmissable: when asserting on source, strip comments or parse the construct. Never grep the raw file.**
 **Suites: rules 59/59, model 27/28, lifecycle 20/20, pilot 29/29, tenant 12/12.** The single failure is N-29, still deliberate.

- **2026-08-13 (working TODO by TODO, one at a time):** Client asked for exactly this: build one, prove it, move on. No batching, no "I cannot get there".
 **N-1, N-2, N-3, seats carry a chosen number of mocks.** A consultancy buys 3, 6 or 10 mock seats and may mix them in one order. **The size travels on the LINK** (`?seat=seat3`), which is what let us keep N-2: sign-in stays Google-only and there are still **no passwords anywhere in this product**. The size is recorded **on the allocation** not read back from the consultancy later, a consultancy can change its seat size between one student and the next, and a student given a 10-mock seat must keep 10 even if the next batch is bought at 3. Reading it live would silently rewrite their history. An unknown code in the URL falls back to the default rather than granting whatever the URL claims (G-2). Proven: seat3→4, seat6→7, seat10→11, `?seat=seat9999`→11.
 **N-4, N-7, N-8, a consultancy student is never sold to.** `seatBacked` is computed on the server from the ledger and returned by both `/api/me` and `/api/account`; the dashboard shows "your consultancy is covering this" instead of a price. Their consultancy already paid, and asking again in front of thirty of their students is the fastest way to lose that consultancy. A student who pays us directly is `seatBacked: false` and still sees prices, proven both ways.
 **My test failed first because it signed up seven students against a five-seat consultancy and ran it dry** so seat10 came back as the bare trial and looked like a broken seat size. It was a broken test. Seat count raised to 20 with a comment explaining why.
 **model-check now 32/33.** The single failure is still N-29.
 **Harness note:** `next dev` in the mirror needs ~20s warm-up plus ~100s for the full model suite; run it backgrounded to a file and read the file, or the bash call times out at 178s.
 **N-5 renewal, and a real bug it exposed.** `allocateSeat` is idempotent per student, correct for a SIGNUP, where a retried request must not hand over two seats. A renewal is the opposite: a deliberate second grant, months later, decided by a human. Without a flag, renewal quietly gave the student credits **without consuming a seat** so a consultancy could top the same student up forever on one seat. `allocateSeat` now takes `{ renewal: true }`. Credits **add** rather than replace: a student with 7 left who is topped up with a 3-seat has 10, never 3, anything else silently takes something they already owned. Proven both ways, plus one consultancy cannot renew another's student (404).
 **N-10, N-11, N-12, N-13, N-20, the QR, the wallet and the WhatsApp escape.** All five live in `PlatformSettings` and are changed by the **super admin only** with no deploy: a wallet number that needs a code release to update is a number that will be wrong on the day it matters most, while a student who has just sent money is trying to reach us. Env vars remain as the fallback so nothing breaks before the first time somebody sets them. A consultancy admin has no such action on their route at all (400). **N-12**: the WhatsApp link opens with the whole message already written, including their payment reference, a frightened student who has just sent real money should never have to compose an explanation in English on a phone. **N-13**: every approval request in the super-admin queue now carries the payer's phone, because when money has not landed the only useful next step is to ring them, and making the approver look the number up elsewhere is how a payment sits overnight while a student assumes they were robbed.
 **HARNESS TRAP, cost one run:** after `rsync` into the mirror, `next dev` served a stale `.next` and every multipart POST to `/api/session/[id]/answer` came back **404 "Failed to find Server Action"** which looked exactly like a broken answer route and was not. **`rm -rf .next` in the mirror before starting the dev server.**
 **model-check 39/40.** N-29 remains the only failure.
 **N-14, N-15, N-16, upgrade, top-up, install.** The decision of what to offer is made on the SERVER (`offerUpgrade`, `offerRenew`), not in the browser. **The top-up appears at two mocks or fewer, never at zero**: earlier is nagging, at zero it is already too late because the student has been stopped mid-preparation. It renders ABOVE the upgrade link deliberately, a nearly-empty student needs the top-up, not the shop, and the other order is how somebody ends up comparing packs while their interview is next week. Seat-backed students are excluded from both (N-4). `lastPayer` pre-fills the next checkout. The install prompt moved onto the **dashboard** because the report is seen once and the dashboard is the page they come back to.
 **N-17, N-18, N-19, the device soft-block.** The super admin can block a fingerprint by hand, which outranks the heuristic in both directions. **It is a SOFT block and stays soft**: the student is still signed in, keeps every report they earned, and can still buy a pack, the only thing they lose is another *free* trial. The message never says ban, fraud or cheat; it says what to do and gives a number. `blockedWhatsappLink()` pre-fills the appeal, because a wrongly flagged student who has to compose an appeal in English on a phone is a real customer we simply lose. Unblocking releases the device immediately, proven in both directions.
 **model-check 49/50.**
 **N-21 to N-24, the super admin directory.** Students and consultancies in **two separate lists** because one mixed list makes the reader do the sorting in their head, the two are answered by different questions ("can they practise" vs "are they buying"), and they are different privacy classes, keeping them apart makes G-8 easier to hold, not harder. Per student: level, target university, WhatsApp number **and whether it really has WhatsApp** (asked, never assumed, half the support plan is a WhatsApp link, and a number that turns out not to be on WhatsApp is a student we cannot reach on the day their payment fails). Per consultancy: seats given out, renewals, students from the link. **N-23 is city only, volunteered at payment, never GPS** proven by asserting no coordinate field exists anywhere on the record. Everything is captured at the payment step, where asking is relevant, rather than gating the free trial behind a form. `N-21b` proves the directory still carries no transcript at any level.
 **model-check 54/55.**
 **N-34, N-36, N-37, practice becomes a real feature.** One question at a time from a practice credit, and **aimed by default at the student's weakest ASSESSED sub-score** via `weakestCategoryFor()`: practice that starts from nothing is a worse product than practice that continues the report they just read, and a student told "you gave no real detail" should land on a question that demands detail without working out which theme that is. Returns null when nothing is scored, and the caller falls back to random, guessing a weakness we never measured would be scoring silence one step removed. **N-37: practice is marked in history and excluded from the mock trend.** A one-question drill averaged with a seventeen-question mock would swing the trend on a single answer and tell a student they had improved or collapsed when nothing of the sort happened.
 **model-check 57/58.**
 **N-25, N-28, question authoring.** The super admin adds questions to the live bank with **no deploy**: the bank IS the product, and waiting for a code release to add a question a student just reported from a real interview is how a bank goes stale, and a stale bank is the one thing that would make this product useless while still appearing to work. Half-written questions are refused (400). **N-28**: students on the NPR 799 pack can add their own, recorded against their own id so their questions never leak into another student's paper. Below that pack it is refused with wording that makes clear **nothing they already have is taken away**.
 **A test-hygiene trap worth remembering:** my insert anchored on `=== PRACTICE ===` and the file contained that marker TWICE, so the whole block was duplicated and node refused to parse it (`Identifier already declared`). **When inserting into a test file by string match, check the anchor is unique first.**
 **model-check 61/62.**
 **N-6, N-9, N-27 done.** N-6: a consultancy buys more seats through the **same shape as a student payment** QR, transaction id, super-admin approval, deliberately not a special B2B flow, so there is one approval queue, one set of money guarantees and one place a mistake can happen. Same one-payment-in-flight guard, so one transfer cannot become two approvals. N-9: checkout order proven **by position in the source** QR, wallet number, details, optional photo, pay, with the WhatsApp escape directly under the button that might fail. N-27: `eligiblePool()` bounds the randomness; a question can only reach a paper if it is in the vetted bank, never generated or paraphrased.
 **model-check 66/67. Eight rules remain: Q-1, Q-2, Q-5, Q-8, Q-9, N-29, AI-9, all of them either the question research or blocked on the AI keys.**
 **N-29, Q-1, Q-2, the question bank now cites its sources, and this was the last non-AI gap.** Two named UK university sources, dated 13 Aug 2026: **Oxford Brookes' own credibility-interview guidance to its offer holders** (which publishes the five topic areas and example questions under each) and **University of Lincoln's Pre-CAS page**. Every category in `lib/data/questions.ts` is mapped to one of the five published topic areas, and the header states plainly that a question fitting none of them is not a Pre-CAS question however sensible it sounds.
 **Two lines from the source now shape the whole product, and are quoted in the file:** *"If you just give general answers that anyone could give, then your visa application may be refused"*, which is the external justification for N-30 forbidding generic feedback, because coaching a student toward a general answer would be coaching them to fail. And *"It is not enough to rely on university rankings"*, so a rankings-only answer must score BADLY on genuineIntent, not well. That is taken from a university's own guidance rather than guessed at.
 **model-check 69/69. Every Part 12 rule is now BUILT+PROVEN.** The only rules left in the whole document are Q-5, Q-8, Q-9 and AI-9, all four blocked on the Groq and Gemini keys, which are still empty in `.env.local`.
 **Full regression after the whole Part 12 build.** Five suites had stale pack sizes baked in from before the 13 Aug price change and failed on a price the client had deliberately set: lifecycle E8 wanted 6, adversarial wanted ≥6, walk 4.6 and 7.1 wanted ≥6, walk 5.4 wanted a 12-mock/30-practice seat and 5.9 wanted ≥19. All corrected to the current packs with a comment naming the change, so the next person does not "fix" the product to match an old test.
 **ALL EIGHT SUITES GREEN: model 69/69, rules 59/59, lifecycle 20/20, pilot 29/29, tenant 12/12, fraud 19/19, journey 26/26, adversarial 18/18, walk 78 steps 0 bugs.**
 **Every rule in `RULES.md` is BUILT+PROVEN except four: Q-5, Q-8, Q-9 and AI-9.** All four need the Groq and Gemini keys, which are still empty in `.env.local`. There is no non-AI work left in the document.

- **2026-08-14 (the 13 commits are pushed, and the AI contract is now proven by EXECUTION):**
 **Pushed.** `b7a25e1..be78ccc`, 13 commits, 180 objects. Netlify rebuilt. The three client-reported bugs that were fixed locally but sitting undeployed are now actually live.
 **Fifteen rules were carrying the wrong status.** M-8, M-9, M-10, M-12 and AI-1 through AI-8 were still marked `BUILT` in `RULES.md` while `model-check.js` had been proving all fifteen at 69/69 for a day. N-30, N-31 and N-32 were marked `BUILT` and were genuinely unproven, no suite cited them. All fifteen are now correct.
 **`qa/contract-check.mjs` is new, and it is a different KIND of test to everything else here.** Every other suite either talks to a server or reads the source. Reading the source is the right tool for "the system prompt lives in code, not a provider console" and the wrong tool for anything about behaviour, **four times on this project a test passed because it matched a comment while the code beneath did something else.** `model-check.js` proves AI-1…AI-8 by grepping `contract.ts`, which is exactly that shape. It is kept, and this suite now stands behind it: it imports the real functions and runs them.
 It feeds `looksGeneric()` an invented quote, a half-quote padded with words the student never said, evidence made only of tiny words (which is also the divide-by-zero case), **every one of the eleven generic phrases we claim to catch, read out of the source so the list and the check cannot drift apart** and the same phrases planted in the Point field rather than the wrap-up. It feeds `buildEvaluationPrompt()` an input object **deliberately polluted with a name, an email, a phone number, a student id, a consultancy, a payment history, a fingerprint and an IP address** the way a careless caller three months from now will feed it, and asserts none of the eight survives to the wire. That is AI-2 and G-8 proven against a hostile caller instead of a well-behaved one.
 **N-30 is proven the way the rule is actually WORDED:** one paragraph is run against two different students' answers, and it cannot pass for both.
 **The suite was verified against nine deliberate mutations of `contract.ts` and caught all nine** approve-everything, drop the quote check, silently skip one generic phrase, drop the Nepali requirement, leak the student name into the prompt, rubber-stamp the shape check, soften the accent instruction, add a translated `nepaliQuestion` field, allow a one-word wrap-up. **A suite that has never been seen to fail proves nothing** so this is now the standard: break it on purpose once before trusting it green. **25/25.**
 It needs **no server, no keys and no network** `node --experimental-strip-types --no-warnings qa/contract-check.mjs`, so it is the first thing to run, and if the contract is broken there is no point starting a server.
 **`SAVE-AND-PUSH.command` replaces the nine one-off push scripts.** Every previous push meant writing a NEW `.command` with the commit message baked in, which is how the folder ended up with `PUSH-NOW`, `CLEANUP-AND-PUSH`, `PUSH-AND-DEPLOY` and six more, any of which can be double-clicked by mistake and commit last week's message. The new one reads the message from `precas-mvp/.commit-message.txt`, which I write first. It also untracks anything now matching `.gitignore` (`--cached`, so **nothing is deleted from disk**), which clears `qa/model-check.js.tmp`, committed by accident on 13 Aug.
 **Harness note for the next session:** the sandbox cannot write `.git/index.lock` or delete files on the mount, so it **cannot commit and cannot `rm`**. Writing and overwriting files works fine. Committing only happens by the client double-clicking `SAVE-AND-PUSH.command`.
 **The document is now: every rule BUILT+PROVEN except four TODO (Q-5, Q-8, Q-9, AI-9, all key-blocked), five `BUILT (browser only)` (S-19…S-23, which need the live Chrome session with the client watching), and M-7 `BUILT (partial)`, which is honest, per-process rate limits need Postgres or Redis to become real.**

- **2026-08-14 (the TESTED walk begins, and stops on a deploy that never happened):**
 **THE LIVE SITE IS SERVING A BUILD FROM BEFORE 13 AUGUST.** `/pricing` on `precasmvp-umanga.netlify.app` still sells **6 mocks for NPR 449 and 12 for NPR 799** and still prints the per-mock comparison table **M-12 deliberately withdrew** "the fairest way to compare is the price of a single mock interview", with rows reading "Ours, NPR 449 · 6 · NPR 75". None of that copy exists in the source any more. Every push reports success and the commit lands on GitHub; Netlify is not publishing it. **Days of work, including the price change the client personally ordered, are not live.** Do not run any further TESTED steps against this host until it is rebuilt, walking a stale build tests code nobody wrote.
 **This is exactly the failure TESTED exists to catch, and no suite could have caught it** because every suite runs against a dev server built from the working tree. Green suites say the code is right. They say nothing about what is being served.
 **Two real bugs found in the first two screens, both the same class.** `/consultancy` promised **12 full mock interviews and 30 practice sessions** a seat while `SEAT_GRANT` grants **10 and 20** the numbers were typed by hand in the JSX and the 13 Aug price change never reached them. On a 30-seat bundle that is **60 mocks sold and not delivered** on the page a consultancy reads before spending NPR 9,000. **M-10 already said a seat is "derived from the 799 pack in code so it can never drift", and it was, in the entitlement code, but not in the sentence that made the promise. A rule enforced on one side of a promise is not enforced.** Four more of the same: "From NPR 449" in `TrialGate`, "NPR 449" in the comparison table, "10 real questions" on the homepage, in the FAQ and on the consultancy landing page. All now read from `plans.ts`.
 **New in `plans.ts`: `ENTRY_PLAN` and `COMPETITOR_ENTRY`.** The second one matters more than it looks, **their entry pack is NPR 799 and our Serious pack is also NPR 799** two identical numbers a few lines apart in one table, one ours and one theirs. If our price changes and somebody updates "the 799 in the table", there is an even chance they change the wrong one and the page silently claims we match a competitor we do not. Naming them differently makes that mistake impossible to make by accident. `COMPETITOR_ENTRY` also carries the date and source it was checked, and the page prints both (G-9).
 **New suite `qa/copy-check.js`.** Reads the source of all 39 pages and components and fails on any product number typed as a literal, price, mock count, practice count, question count. Comments and import lines are stripped first. No server, no keys, no network.
 **Its own mutation test found a flaw in it, which is the point of mutation testing.** Version one matched only the digits currently in `plans.ts`, so re-inserting the real bug, "12 full mock interviews", **did not trip the mock-count check, because 12 is not a pack size any more.** A check that only recognises correct numbers cannot by construction find a number that has gone out of date, and **the stale number is the entire danger.** It now rejects every hand-typed digit in that position; derived values render as `{...}` and never match. Re-verified against six mutations including a stale price nobody sells and the bug hidden inside a comment, caught all five real ones, correctly ignored the comment. **8/8.**
 **Netlify's own Blobs browser is a G-8 problem and the client should know.** The dashboard lets whoever holds the Netlify login browse and download every blob in `precas-db`, and sessions, including transcripts, are stored there in plaintext. Nothing in the product can stop that; it is below the application. Either the Netlify account holder is accepted as outside the admin model, or transcripts are encrypted at rest so the browser shows ciphertext. **Client decision, not a code decision.**

- **2026-08-14 (why nothing was deploying, and why nothing runs locally either):**
 **THE DEPLOY CAUSE IS BILLING, NOT CODE.** The Netlify dashboard says it in one line: *"Umanga Niroula's Team is now running on operational credits. Your published sites are still live, but production deploys and Agent Runners are paused... Upgrade your team or wait for your next billing cycle to resume."* Pushes succeed, GitHub receives every commit, and Netlify simply does not build. **Nothing is wrong with the repository and nothing is wrong with the pushes** so a fresh repo or a second Netlify account would fix nothing, the limit is on the team, and a second free account under the same person is dodging a billing limit while re-entering every secret for a host we are leaving anyway. **Client decision, recorded: leave Netlify alone, test locally, move to the VPS when it arrives.**
 **THE CLIENT'S SIGN-IN BUG IS THE OLD BUILD.** They reproduced it live and were right to be annoyed that it was still there: first Gmail said "signing you in" and did nothing, a second Gmail signed in, and then **every university button reverted to "Sign in to start" with no name and no sign-out shown**. That is the PILOT-01/PILOT-02 loop. **It was fixed in code days ago and the fix has never once been served.** My mistake was narrower than "I did not check": I proved the fix against a local dev server, reported it fixed, and never confirmed what the host was actually serving. **Proving the code and checking the deploy are two different jobs, and I only ever did the first.** Still unverified as of tonight, it must be re-walked locally.
 **NOTHING RUNS LOCALLY EITHER: this Mac has Node 20.6.0 and Next.js requires >= 20.9.0.** `npm run dev` prints "You are using Node.js 20.6.0" and exits, so the server never starts. This is almost certainly why local testing has never been a real option on this machine, and it is why every suite has only ever been run by me in a sandbox on Node 22, **the client and I have never been running the same software.** `CHECK-NODE.command` (read-only) found exactly one Node at `/usr/local/bin/node`, no nvm, no volta, **Homebrew 6.0.11 present**.
 **`START-PRECAS.command` now checks the Node version before doing anything** and stops with a plain explanation rather than letting Next.js fail in a way that looks like a broken app. It also prefers `node@22` **for its own window only** never a PATH change for the whole machine, so no other project on this Mac is disturbed. And it now **deletes `.next` before starting** which is the stale-build trap that has already cost one full run.
 **New scripts:** `CHECK-NODE.command` (read-only survey), `INSTALL-NODE.command` (`brew install node@22`, side-by-side). **Correction owed and recorded:** I told the client "nothing else on your Mac changes". Homebrew then reported it would also **upgrade 10 shared dependencies** brotli, icu4c, libnghttp2, ca-certificates, openssl@3, readline, sqlite, lz4, xz, zstd. That is normal Homebrew behaviour and low risk, but it is more than I described, and the description was wrong before the install ran rather than after.
 **Blocked on the client at Homebrew's own `[y/n]` prompt.** Terminal is granted click-only, so I cannot type into it, which is correct here: an install that upgrades ten shared libraries should be approved by the person who owns the machine, not waved through by me.

- **2026-08-14 (Node installed, server running, and the client's sign-in bug finally found):**
 **Node 22.23.2 installed side-by-side via Homebrew; the old 20.6.0 is untouched at `/usr/local/bin/node`.** `START-PRECAS.command` runs, the app serves on `localhost:3000`, and the client signed in with Google against the local server. **This is the first time the client and I have been running the same software.**
 **THE SIGN-IN BUG IS REAL, IT IS FOUND, AND IT IS FIXED.** Reproduced in the first two navigations: `/` showed "Jhattaro · My practice · Sign out", `/universities` showed "Sign in · Start free", same session, one navigation apart. `HeaderSession` held one piece of state, `signedIn: boolean | null`, and rendered with `if (signedIn !== true) return <SignedOut/>`. **That single line collapses `null` ("I have not asked yet") and `false` ("I asked, nobody is signed in") into one, and renders both as signed out.** So every signed-in student was told "Sign in" for as long as `/api/me` took, **measured at over four seconds on localhost** worse on Nepali mobile data, on every navigation rather than as an edge case.
 **It is G-1 in a different costume: do not assert a state you have not established.** G-1 refuses to score an answer it could not hear. This now refuses to claim a session state it has not confirmed. The old comment on that line said "never flash Sign out at somebody who is not signed in" and was right about the reverse being bad, but the cure was worse, because flashing "Sign in" at somebody who IS signed in tells them the product forgot them, **which is the exact fear PILOT-01 was about.**
 **Why I missed it twice, stated plainly:** I read the code, the code looked right, and I closed the report. The state machine is only wrong when you watch it in time. **A component with a loading state cannot be reviewed by reading it, it has to be run.**
 **The fix has two halves.** `undefined` is now its own state rendering a fixed-size neutral placeholder (nothing lies, nothing jumps). And **server pages resolve the session server-side** via `lib/auth/header-session.ts` and pass it to `SiteHeader`, so the first paint is already correct with no unknown phase. Client pages (`/universities`, `/account`, `/practice`) cannot, a server component imported into a client page becomes a client component, so they get the honest placeholder. Also: **a failed `/api/me` no longer overwrites what the server already told us** because a flaky request is not evidence of being signed out.
 **Verified in the browser: frame one is the placeholder, frame two is "Jhattaro · My practice · Sign out". No wrong state at any frame.**
 **New suite `qa/header-check.js`, 8/8** verified against six mutations including restoring the original `signedIn !== true` verbatim. **H-7 asserts every SERVER page that renders `SiteHeader` actually passes the session** because a page that forgets would flash and nobody would ever notice.
 **STILL TO WALK: the interview room, S-19 to S-23.** Camera and mic permission, the meter reflecting real recorded audio, the question in writing, the countdown and violation count, and noise not spamming the monitor. **These need the client's microphone and camera and cannot be done without them.** Everything up to the room door is now walked.

- **2026-08-14 (the worst bug of the walk: pressing Back cost a student their whole free trial):**
 **What happened:** the client started a mock, answered ONE of his ten free questions, pressed the browser Back button by mistake, and every page told him **"You have used your free questions. Buy a pack to keep going."** His interview was whole and one click away.
 **Why:** `consume()` spends the credit on the FIRST answer. That is *correct*, otherwise one credit buys seventeen questions across seventeen abandoned tabs, but it means an **abandoned** sitting and an **exhausted** balance produce the identical number, `mocksLeft = 0`. The resume machinery already existed (`/api/account` `resumeHref`, `/api/session/[id]` "a closed tab must never cost a student their session"). **Nothing was broken except what the product SAID.**
 **This is the same defect as the header flash, one layer up: we rendered a CONCLUSION where we should have rendered a STATE.** `mocksLeft = 0` is a fact. *"You have used your free questions"* is a conclusion, and it was false. The client's own framing is the rule to keep: **a sitting is not spent until it is finished** it applies identically to a paid 17-question mock, where pressing Back would have cost a real NPR 449.
 **Fix, three layers deep so no single page has to remember:**
 1. `Entitlement` gains **`inProgress`** sessionId, answered, total, institution, isPractice, computed from the store and wrapped in try/catch so a store hiccup can never become "buy a pack".
 2. `reason` checks **`inProgress` FIRST** so the sentence cannot be produced while a sitting is open. `canStartMock` is true when a resumable mock exists, because it *is* the mock they already paid for.
 3. `/api/session/create` **resumes the open sitting instead of creating a second one** including when they tap a different university, because they still have one sitting open and one credit spent. Creating a second session would have taken *another* credit for questions already paid for.
 **And the page says it before they tap:** a green "You have an interview in progress, you answered 1 of 10 questions. Nothing is lost" banner above the search, and every card reads "Continue your interview" with an "In progress" chip instead of "Needs a pack".
 **Verified live in Chrome** on the client's own abandoned session: `/api/me` returns `inProgress {answered: 1, total: 10, inst-bpp}`, `reason: null`, `canStartMock: true`, and the banner renders.
 **Harness note that cost three screenshots:** in dev, a screenshot taken 8-10s after navigation can still catch the **pre-hydration** frame, the placeholder and the signed-out card labels, and look exactly like a broken fix. `read_console_messages` showed no errors and a direct `fetch('/api/me')` in the page returned correct data in 64ms. **Confirm via the DOM (`document.body.innerText`) before believing a screenshot in dev.**
 **The client's standing question, recorded: "what are other rules such as this that can be broken?"** Both bugs found tonight are one shape. Task #2 is the sweep.

- **2026-08-14 (the Back button, part 2, the room itself was losing questions):**
 **`Q 8/10 · 1 done, 9 left` was the bug printed on screen.** A browser-side index had walked forward eight times against **one** real answer, and the two numbers were never required to agree. `currentIndex` was a **stored cursor** written down as the student moved, and **Skip was browser-only** it moved a number and told the server nothing. Consequences the client hit in minutes: press record on Q2, go Back before speaking, come in again and land on **Q3 with Q2 gone**; and a skipped question returns forever because the server never learned it was passed.
 **`lib/resume.ts`, the resume point is now DERIVED and never stored.** It is computed from what actually exists: answers submitted and skips taken. **Derived state cannot drift, because there is only one of it.** New `POST /api/session/[id]/skip` records a skip server-side, idempotently, and refuses a questionId that is not in that paper (so a crafted request cannot fast-forward to the end). `GET /api/session/[id]` now returns `resumeIndex` and `answeredCount`, and the room starts from `resumeIndex`.
 **THE RULE, and it answers the client's question directly: a question is done when audio was SUBMITTED, or when the student chose to skip. Nothing else counts.** He asked whether an attempt should start counting after some number of seconds of recording. **It should not, and no threshold is needed** a threshold is one more number to get wrong, and it would have to be defended to the student whose recording was cut at nine seconds by a power cut. Submitted audio is the line. G-1 already refuses to score audio it could not hear, so a submitted-but-silent answer is handled honestly further down and is not the resume logic's problem.
 **A skip is deliberately NOT filed as an answer:** no attempt consumed, no transcript, never scored. Filing it as a bad answer would score a student on something they explicitly declined to do, G-1 in another costume.
 **E-4 violation fixed: demo mode blamed the student.** With no speech-to-text key the transcript is sample text, so the answer was never listened to, and the screen still said **"We could not use that answer"** three times in a row. That tells a student their speaking was the problem when the problem is a missing key of ours. It now says "Demo mode: we are not listening yet" when `demo.stt` is true.
 **A quiet recording now says to speak up.** Speaking softly produced nothing, and the only guidance was to check the microphone was on. **His microphone was on.** The one thing he needed was "speak louder", so that is now the first sentence, with the reason attached, the officer will need to hear him too.
 **Verified NOT a bug, having been suspected:** signing out and back in with the same Gmail does **not** re-grant the free trial. `evaluateTrial` returns `already_claimed` and `grantTrial` is never reached. Worth recording because it looked like a money leak and is not.
 **Confirmed correct in the walk:** the second Google account, which genuinely finished its ten questions and has no open sitting, still correctly reads "You have used your free questions." The `inProgress` fix distinguishes the two cases rather than suppressing the message.
 **STILL OPEN (client's list): the sweep (#2), checkout QR + super-admin upload (#3), 449/799 switcher at checkout (#4), the confusing "last 4 digits" field and real transaction-ID examples (#5), WhatsApp number + logo shown in plain text everywhere with a super-admin setting (#6), hiding the live transcript (#7).**
 **Client also asked for a dismissible POPUP on every page for a student who has used their trial. Not built, and it needs a decision** a blocking interstitial on every page is the pattern this product positions itself against, and it will cost trust with the exact audience that has not paid yet. A persistent dismissible banner, or one modal per session rather than per page, gets the same reminder without the dark pattern.

- **2026-08-14 (three more from the walk, and two of them were my own scripts lying):**
 **THE SUPER ADMIN PASSCODE IS `change-me-super`. THE OWNER KEY IS `change-me-owner-different`.** Both are set in `.env.local`, which **disables** the `super-dev` / `owner-dev` fallbacks, those only apply when the env vars are unset. **`START-PRECAS.command` printed `super-dev` unconditionally** so the client sat in front of `/super` typing a passcode my own script told him was correct, getting "Not allowed". The script now reads `.env.local` and prints what actually works, or says plainly that it is quoting a fallback. **A script that prints a confident wrong answer is worse than one that prints nothing.**
 **`components/PasscodeInput.tsx`, masked by default, revealed on a deliberate press.** A masked field with no reveal cannot be debugged by the person using it: he could not tell a wrong passcode from a typo, a stuck modifier or a keyboard layout eating a character, and the only feedback was two words of red. On `/super`, `/owner` and `/admin` the realistic failure is not a shoulder-surfer, it is **the owner locked out of his own back office at one in the morning.** `autoComplete="off"` so a shared back-office passcode is never offered to a browser profile that may not belong to the holder. **Never used for students, they have no passwords anywhere and never will (N-2).**
 **THE WORST OF THE THREE: "Choose Prep" did not go to checkout.** It was `href="/start"` for everybody. `/start` is the sign-in page, and it bounces an already-signed-in student to `/universities`. So **the one button on the site whose entire job is to take money went to the catalogue** including for a student who had used all ten free questions and was actively trying to pay us. Now: signed in → `/checkout?pack={code}`; signed out → `/start?next=/checkout?pack={code}`, so the pack they chose survives the sign-in detour. `/start` already honoured `?next=`, so nothing else was needed. **`signedIn` is passed in from the server page, not fetched in the component, a price button that changes its destination a second after paint is a button somebody has already tapped.**
 **The client's standing instruction, recorded: map EVERY button.** Who is clicking, what state they are in, and where they land. The pricing CTA is the proof that this is not busywork, it was wrong for every visitor, on the highest-value control in the product, and no test looked at where a `<Link>` points.

- **2026-08-14 (super admin could not approve a payment at all, and the dashboard you cannot act on):**
 **"Too many attempts. Please wait five minutes." on the FIRST click of Approve.** Cause: `LIMITS.auth` (5 per 5 min) was applied to **every** `/api/super` call, including reads. Opening `/super` fires four actions, overview, students, payments, flagged, so **loading the page spent four of the five tokens** and the next click was refused. **`auth` is a brute-force budget, and brute force means GUESSING, a guess is a FAILED attempt.** Charging a correct passcode the same price as a wrong one locks out the only person who is meant to be there and does nothing to an attacker that counting failures would not do better.
 **Fix:** `rateLimitPeek()` (checks without consuming) before auth, `rateLimitPenalise()` **only when the passcode is wrong** and a new generous `LIMITS.backOffice` (240/min) for authenticated work so a runaway retry loop is still bounded. **Applied to all three back-office routes: `/api/super`, `/api/admin`, `/api/platform`.**
 **Two contradictory banners on screen at once** "Too many attempts" directly above "Payment verified and the pack was added", because `loadAll()` re-set the error after the success. `call()` now clears BOTH at the start and clears the notice whenever it sets an error. **A screen showing two mutually exclusive outcomes is worse than one showing neither.** The row also stayed `submitted` while the dashboard already counted the payment, which was the same stale `loadAll()` being rate-limited.
 **NO PHONE NUMBER ANYWHERE IN THE BACK OFFICE, and the API was already returning it.** `/api/super` `directory` returned `whatsappNumber`, and the payments branch returned `payerPhone` and `payerPhoneSuffix` (N-13). **The UI never read any of them.** Same shape as the consultancy page: the server did the work, the screen never used it. Added a **Phone** column to both the students and payments tables, as a tappable `tel:` link, with `paid from ...1234` underneath in payments (that is the bit you check against the wallet ledger) and a **"not on WhatsApp"** warning where `whatsappConfirmed === false`. **A directory you cannot act on is a list, not a tool.**
 **Client decision recorded: NO dismissible popup for exhausted students.** He rejected it outright. Do not build it.
 **Standing instruction: do not stop, do not report between tasks, keep this file current because the session may be continued by another AI.**

- **2026-08-14 (checkout, super admin settings, contact, and the recording room):**
 **`components/PaySettingsForm.tsx` + a new "Payment details" tab in `/super`.** `setPaymentSettings` had worked in `/api/super` since the day it was written and `PlatformSettings` held every field, **there was simply no form anywhere that called it** so the QR, wallet number and support number could only be changed by editing env vars and redeploying, which is exactly what N-10 and N-11 exist to prevent. **That is the THIRD time in one night: the server did the work and no screen used it** (consultancy seat numbers, the payer phone, and now this). Worth naming as a pattern, a green suite happily proves an endpoint works while the feature does not exist to the person who needs it. The QR accepts a pasted URL **or an uploaded file** read in the browser to a data URL, so it needs no bucket, no upload endpoint and no credentials, and behaves identically on this Mac, on Netlify and on the VPS. Over 400 KB is refused with a reason, never silently truncated. The saved QR is **previewed at the size a student sees it** because an admin has to be able to confirm it is the right QR before students are pointed at it.
 **`components/ContactUs.tsx`, one contact block, used everywhere.** The client tapped "Message us on WhatsApp", it opened with no prefilled text, and **the number itself was written nowhere on the page**. A button is a bet that one app on one phone behaves. This always shows three things: the WhatsApp mark (inline SVG, no CDN), the button with the message prewritten, and **the number as dialable, copyable text**. Used on checkout under "I have paid" and on the payment-in-flight screen with `urgent` framing, that screen previously offered nothing, and a student who has sent real money and heard nothing has exactly one question, "who do I call".
 **The in-flight screen was reading `process.env.NEXT_PUBLIC_SUPPORT_WHATSAPP` directly** so changing the support number in `/super` would not have changed it. New `lib/support.ts` resolves the super admin's setting with the env var as fallback, and `SiteFooter` and `/consultancy` now use it. **`/start` still reads the env var and is the one place left to convert** it is a client page, so it needs the value passed in or fetched.
 **Checkout: the pack switcher.** NPR 449 and NPR 799 side by side as radios, read from `plans.ts` (never typed, `copy-check.js` would fail the build). Changing it re-prices via the server, and `pack` is now in the effect's dependency list, because without it the radio would move and the amount would not: **the same two-sources-of-truth defect as everything else tonight.** The URL is kept in step with `replaceState` so a refresh does not revert the choice. **Why this matters beyond convenience: the alternative was pressing Back, and Back is the action that has already cost this product a mock and a free trial tonight.**
 **Checkout field guidance rewritten from the client's three real receipts.** The placeholder was `0011ABCD2233`, which matches no wallet anybody in Nepal uses. Real ones: eSewa **Transaction Code `1NOH8C2`** (7 alphanumeric), Nabil Bank **Transaction ID `697873804`** (9 digits), mobile banking **Reference Code `395407924`** (9 digits). The **label** varies as much as the format, so the page now names all three, a student hunting for "transaction number" on a receipt that says "Reference Code" reasonably concludes they are on the wrong screen. And "Last 4 digits of the number you paid from" became **"Last 4 digits of your phone number"** with a worked example, because "the number you paid from" was ambiguous between three different numbers all printed on the same receipt.
 **The interview room no longer shows your words as you speak.** Client's call and he is right: watching your own half-finished sentences appear is a live self-monitoring loop, a nervous speaker stops and corrects and loses the thread, and the real interview has no such display, so practising with one trains a habit that will not be there on the day. **The live signal is not thrown away.** It now drives one thing: after 5 seconds of recording with nothing picked up, the panel says **"We cannot hear you yet, speak louder, and closer to the microphone"** because the client found that speaking quietly produced nothing while the screen said "Listening", which reads as working when it is not. Guarded by `liveSupported`, so Firefox and Safari (no SpeechRecognition) never cry wolf, the server transcript still works there.

- **2026-08-14 (session 5, part 1, the features that had no door):**
 **Baseline first.** Rebuilt the /tmp mirror (it does not survive a new sandbox), ran all thirteen suites on fresh servers, `tsc` clean, `next build` clean. Two suites were red and **both were stale tests, not product regressions** which is worth recording because the instinct is to "fix" the product back into a bug.
 `model-check` N-9b searched the checkout source for the literal sentence "Something wrong? Message us on WhatsApp". That inline markup had been replaced by `<ContactUs/>`, which is strictly better, so the test went red while the product got safer. Rewritten to check the GUARANTEE: the escape hatch sits below the button that can fail, **and** it is not merely a button, because a WhatsApp link is a bet that one app on one phone opens. Added N-9c for the dialable number.
 `pilot-check` CS-04 asserted a flat 402 the moment the trial credit was spent. The honest reading of the red was not "a student can take a second free mock", it was "this student never FINISHED their first one", they answered all ten and never completed, so the sitting was open and the product deliberately hands it back rather than charging twice. Split into CS-04a (an open sitting resumes, same session id, `resumed: true`), CS-04b (a different university resumes too and names which one), CS-04c (they can finish it) and CS-04 (after finishing, a second mock is refused). **Stricter than the original, not weaker.**
 **THE CLIENT'S "TOO MANY ATTEMPTS" WAS ALREADY FIXED, BUT NOTHING PROVED IT.** New `qa/backoffice-check.js` walks his exact sequence rather than the endpoint: open /super (three actions on mount), click Approve, watch the page reload everything. Under the old five-per-five-minute budget that is 3 + 1 + 3 = seven requests, so the reload was refused, which is also why the row still said "submitted" while the dashboard already counted the money. 12 cases, including that a WRONG passcode is still throttled hard and that a throttled address cannot get in with the RIGHT key either.
 **AND THE PHONE NUMBER HE ASKED FOR WAS STILL NOT THERE.** The payments queue read `st?.phoneE164`, which only Firebase PHONE auth ever writes. Everybody signs in with Google, which carries no number, so **that column was empty on every real payment**. The number a student actually gives us at checkout is stored as `whatsappNumber`, and the directory branch three hundred lines above already read it correctly. Fixed to `whatsappNumber ?? phoneE164`, plus a "not on WhatsApp, call instead" warning on the row.
 **TEN SERVER FEATURES HAD NO SCREEN AT ALL.** This is the fifth, sixth and seventh time in this project (`consume()`, `allocateSeat()`, the consultancy approval queue, `setPaymentSettings`, the payer phone). So it is now **enforced**: `qa/reachable-check.js` fails when any API action has no caller in any page or component, when a repo method has no caller, or when a tab renders nothing. Anything genuinely screenless must be listed WITH A REASON.
 Built the missing screens: **super admin** got a Consultancies tab (create, approve, suspend, seat rollups, the entire consultancy channel could previously only be opened by hand-writing an HTTP request), an Audit tab, a Questions tab, Give credit and Stop free trials from this device on the student and flagged rows, and a form for the post-trial offer. **Consultancy portal** got Buy more seats, Change how your page looks, and Use a seat to top a student up.
 **`submitSeatPayment` did not exist on the server either.** `buySeats` created the order and showed the QR, and a consultancy could send NPR 9,000 with no way to tell us the transaction number, so the order sat in `created` for ever. Added, mirroring the student path exactly: one txn claimed once, and the same details twice answered calmly rather than in red.
 **A MONEY HOLE I OPENED AND CAUGHT BY RUNNING IT.** A consultancy's own seat order carries their consultancy id, so the existing ownership check let them approve it themselves: order twenty seats, claim to have paid, approve yourself, take NPR 6,000 of stock. `/api/admin` approve and reject now also require `order.studentId`, so a seat order is only ever the super admin's to approve. **Reading the code would not have found this. Running the flow did.**
 Also wired `upsertRewardRule`, which had never had a caller, so `RewardRule`'s own comment ("a reward rule the super admin controls") had been untrue since it was written. The window is clamped to 15 minutes to 24 hours and the public reason cannot be blank, because this is the honest-countdown feature and a lever that could turn it into fake urgency is worse than no lever.
 **New suites: `backoffice-check.js` 12/12, `backoffice-ui-check.js` 18/18, `reachable-check.js` 6/6.**

- **2026-08-14 (session 5, part 2, the AI guarantee, and the docs brought in line):**
 **Phase 2 is 2 of 4, not 0 of 4, and the two that are done are the two that do not need the client's money.** New `qa/ai-check.js`, 17 cases, all on L4: **no fabricated score ever reaches a student.** Silence refused before we pay to transcribe it; a two word answer never scored; the report never printing NaN; Whisper's stock subtitle hallucinations on silence caught and treated as silence; personal numbers redacted before anything is stored or sent to a model; with no key in PRODUCTION the transcriber fails loudly and the evaluator returns nothing rather than a mock; and the spend ceiling checked BEFORE the paid call rather than after, because checking afterwards means the call was already paid for.
 **The owner could not tell whether the AI was on.** The only way to find out was to sit an interview and look for the demo banner, which is the wrong person doing the wrong test. `/super` now shows speech-to-text and feedback as Live or Not on, the provider name, and paid calls this month against the ceiling, plus an amber panel across the top when it is off. **No key is ever returned to the browser** only whether one is present.
 **`userMessage` was outside the typed response shape.** On a too-short or silent answer the route returned `{...payload, userMessage}`, a user-facing field spread in beside `data` that `ApiResult` does not describe. It worked only because the one component that reads it happened to know. Moved inside `data`, typed, and InterviewRoom updated. A field that exists by convention is one refactor away from silently vanishing, and the student would then be told nothing about why their answer was not used.
 **`copy-check` caught me typing prices by hand** in the new seat purchase copy ("12 mock interviews and 30 practice questions ... NPR 799"). Exactly the two-sources-of-truth fault that suite exists to stop. Now reads `SEAT_GRANT` and `getPlan('serious')`. **Worth noting that the suite caught its author.**
 **Docs brought in line with reality.** `docs/TEST-PLAN.md`: the suite table now lists all 17 with sizes and whether each needs a server; Part D no longer lists D-01 to D-03 as open, because they are fixed, and instead names what is genuinely open (O-1 no AI keys, O-2 no accent benchmark, O-3 six items needing a real handset); Part A gained every control built tonight; and a new **section 7b names the recurring defect class** with all seven instances and what the guard does and does not catch.
 `CHECKLIST.md` now carries the real numbers and four new client decisions: no dismissible popup, API keys stay out of the dashboard, a consultancy may never approve its own seat purchase, and the offer window is clamped to 15 minutes to 24 hours.
 **FULL REGRESSION, 14 August: 453 checks across 17 suites, all green.** walk 78, model 70, rules 59, pilot 32, journey 26, contract 25, lifecycle 20, fraud 19, adversarial 18, backoffice-ui 18, ai 17, backoffice 12, tenant 12, copy 8, header 8, reachable 6, route 5. `tsc` clean, `next build` clean.

- **2026-08-14 (session 5, part 3, committed as `d3b784c`, ONE DOUBLE-CLICK FROM LIVE):**
 **Everything is committed. Nothing is uncommitted. The push is the only thing left, and it needs the client's Mac** because this sandbox holds no GitHub credentials (`could not read Username for 'https://github.com'`) and computer-use needs him awake to approve the access dialog.
 **What he does when he wakes up: double-click `PUSH.command` in the `Content Karkhana` folder.** Nothing else. It already clears the stale git locks, forces HTTP/1.1 with a large post buffer (the fix for the old `RPC failed; HTTP 400 curl 22`), pushes `master:main`, retries `master:master`, and prints whether it worked. Netlify rebuilds in three to five minutes. `git log origin/main..master` is exactly one commit: `d3b784c`.
 **A git trap solved properly this time, write it down.** The mount cannot delete `.git/index.lock` ("Operation not permitted"), so the moment any git command leaves one behind, every later git command refuses to start. `rm -f` does not work. **The fix is `GIT_INDEX_FILE=/tmp/precas-index`**: point the index somewhere writable, `git read-tree HEAD`, `git add -A`, `git write-tree`, `git commit-tree`, then `printf '%s\n' "$COMMIT" > .git/refs/heads/master`. That never touches `.git/index.lock` or `.git/HEAD.lock` at all. `git fsck --connectivity-only` is clean afterwards, so the objects really are written despite the `unable to unlink tmp_obj` warnings, which are noise from the same mount limitation.
 **`git status` on his Mac will look alarming before the push, and it is lying.** Because the mount would not let git write `.git/index`, that file is stale: it still holds the state from before these two commits, so `git status` shows `MM` on everything and `git diff HEAD` claims the six new suites were deleted. **They were not.** `git ls-tree HEAD qa/` lists all nineteen files, and a tree computed fresh from the working directory hashes to `1b13277`, byte for byte identical to the committed tree. The push only reads refs, so it is unaffected, and `rm -f .git/index.lock` at the top of `PUSH.command` works on a real filesystem, after which git rebuilds the index on the next command and status goes normal. **Do not "fix" this by resetting or checking out anything.**
 **New: `qa/run-all.sh`.** One command for the whole gate. It encodes the two traps that have already produced false results here: a fresh server for every server-backed suite, and `next dev` never `next start` (which sets NODE_ENV=production, correctly disables the `dev:` sign-in token, and makes every assertion fail for the wrong reason). It also prints, on success, that green suites are **not** a release, because not one of them loads any JavaScript.
 **Still open and honest about it:** O-1 the AI keys are not set, O-2 the Nepali-accent benchmark has not run, O-3 six items need a real handset or real students. All three are in `docs/TEST-PLAN.md` Part D, and the first is now visible to the owner as an amber panel on `/super` rather than something a student discovers.

- **2026-08-14 (session 5, part 4, BUILT + PROVEN, and the honest audit behind it):**
 **The client asked the right question: "have you actually proven that, or did you write proven for the sake of writing?"** So I measured it instead of asserting it. A script classifies every `t(...)` in every suite by whether its condition touches a variable that came from `fs.readFileSync`. **Result: 501 assertions, 458 drive the running product, 43 only read source.** Those 43 are now marked **SOURCE-CHECKED, not PROVEN** in `RULES.md`, each with the reason, and the ones that genuinely cannot be driven (they need `NODE_ENV=production`, or a real provider, or a real passport number in a transcript) say so on the line.
 **New `RULES.md`: every rule in the product with an honest mark.** BUILT + PROVEN / BUILT + SOURCE-CHECKED / BUILT, NOT PROVEN / NOT BUILT. Part 6 lists what is genuinely not done, and Part 7 lists all 43 weaker assertions by name. **Nothing is marked proven because the code looks right.**
 **Built and proven this session, all from the client's own list:**
 **Passcodes (`qa/passcode-check.js`, 24 cases).** The super admin sets a consultancy's first passcode, so the super admin knows it: a secret two organisations share cannot establish who leaked a student list. It is now a **HANDOVER code** that opens the door once, and the portal refuses every other action until they replace it. Not a dismissible banner, because a banner leaves the shared secret in force for as long as it is ignored. Changing it needs the current one, so a walk-up at an unattended browser cannot take the account. The super admin can now change their own without a deploy, which was previously impossible: not when a laptop is lost, not when somebody leaves.
 **AND I FOUND A LOCKOUT BUG IN MY OWN DESIGN BY WRITING THE TEST FOR IT.** The screen promises "if you forget it, change SUPER_ADMIN_PASSCODE in Netlify and redeploy". **That promise was false.** The stored passcode took precedence unconditionally, so rotating the deploy key changed nothing and the owner would have been locked out of their own product permanently. Fixed by stamping the stored passcode with the deploy key it was chosen against and ignoring it once that key changes. Proven by PC-22 and PC-23 with a dev-only rotate helper that **returns 404 in production**.
 **State (`qa/state-check.js`, 20 cases).** All four places the client named: the free ten, a paid mock, a practice question, and the checkout. Answer one of ten, leave, come back: same sitting, 9 left, no second credit, the answer still there. Also fixed: an opened-but-unanswered sitting is now REUSED rather than leaving an orphan behind on every reload, the same fault as the checkout writing a new order per visit.
 **The soft no.** The rejection reason was already in the status response and the checkout threw it away, so a student saw a generic sentence while the approver's actual words sat unused. Now shown, plus a way to reach a person, plus a fresh order to retry with. The waiting screen states **how many hours** to allow instead of "a little time", and that number is editable in `/super` because the honest answer changes at Dashain.
 **Partial speech (`ai-check` AI-18 to AI-23).** The client's point about weak spoken English. We now detect when we clearly caught less than they said (words per second against duration), say so **in our own name** never mention their accent or their English, name the one mechanical fix (closer to the microphone), and still give feedback on the part we did hear.
 **Leaving mid-interview.** The browser warning only fired while recording, and never for the in-app Back button, which is the one people press. Now covers both, framed as the client asked: treat it as the real interview. **BUILT, NOT PROVEN**: `beforeunload` and `popstate` cannot be driven from a test process, so it is marked honestly and needs the browser pass.
 **The new gate broke six suites, which is exactly what it should have done.** Every suite that created a consultancy and logged straight in was skipping the handover step a real admin cannot skip. All six now change the passcode first. **One of those fixes exposed a vacuous pass**: a stale passcode made a login 403, `orders` came back undefined, and "the queue is empty" passed for the wrong reason. An assertion satisfied by getting nothing back is not an assertion, so it now proves it is logged in before judging what it shows.
 **FULL REGRESSION: 501 checks across 20 suites, all green. `tsc` clean, `next build` clean.**
 **NEXT: the browser pass, with the client watching.** He has asked for it terse: one instruction at a time, click this, check that, and nothing else. Priority order he set: (1) test everything, (2) test everything, (3) the AI, (4) whether the AI works, (5) deploy. He will supply the Groq and Gemini keys today from NPR 7,000, and wants a running record of what each call costs.


- **2026-08-14 (session 6, THE BROWSER PASS, record only, nothing fixed):**
 **The client's instruction for this session, and it is binding until he says otherwise: *"right now, we are just gonna record and continue to push into other things that are going wrong."* Fix NOTHING until the pass is finished.** The live sheet is `docs/TEST-RUN.md`: 120 rule rows, a defect table, and a numbered progress log. **46 of 120 rules tested: 41 PASS, 4 FAIL, 1 BLOCKED. 19 defects open, 1 withdrawn.**
 **THE HEADLINE: the money path itself works, end to end, proven live.** Pay NPR 449, submit a transaction number, land in the queue, approve, and **the student's very next request already reads `hasPaid: true`, `mocksLeft: 3`, `practiceLeft: 15`, `questionsAllowed: 17`** (up from 10 on the trial), with the half-finished trial session preserved through the purchase. Approving the same order twice answers *"This payment was already approved. Nothing changed."* and grants once. **S-46, S-47, S-48, S-49, S-53, SA-2, SA-3, SA-4 all PASS.** The owner kill switch is fully proven too: the super key is refused at the owner's door, six routes 503 while paused, students see the message and the phone number, both doors stay open so there is no lockout, and every pause and resume is logged with time and address. **O-1 to O-6 all PASS.**
 **SIX CRITICAL DEFECTS BLOCK THE LAUNCH. In fix order:**
 **D-15, the consultancy portal cannot be entered by anyone, ever.** A new consultancy signs in with the handover code and is correctly told to choose its own passcode. **There is no field to choose one.** The server gate refuses every action except `changePasscode`, `login` is not on that list, so `login` 403s; `app/admin/page.tsx` only sets `data`/`mustChange` when `login` SUCCEEDS, and the forced screen is guarded by `if (data && mustChange)`, so it never renders. Proven by API: `login`→403, `changePasscode`→200, `login` with the new code→200. **The server is entirely correct. Only the screen is wrong.** Fix: let `login` through the gate, or render the forced screen from the `PASSCODE_MUST_CHANGE` code instead of requiring `data`.
 **D-16, a consultancy's private passcode is handed to the super admin in plain text.** After the consultancy replaces the handover code, `/api/platform overview` returns `"passcode":"everest-own-2026"`. Stored in plain text too, since it can be read back at all. **This makes rule A-18, "Their passcode is never sent back to the browser", marked PROVEN, false.** `/api/admin` does strip it, which is what the existing test checked; nobody checked `/api/platform`. Fix: hash it, compare with `secretEquals`, never put `passcode` in a response.
 **D-17, the checkout locks a paying student out, and its own waiting screen is what does it.** The limiter sits at the top of `POST /api/payment` before any action branching, so it charges `create` and `status` as well as `submit`. The page fires `create` on mount and on every pack change. **The waiting screen polls `status` every 8 seconds against a budget of 10 an hour, so it locks the student out after 80 seconds** and they then cannot reopen the checkout. When it trips, the page renders a pack picker and a red error and **the "Talk to a person" card disappears** the hardest dead end in the product, on the money path. The 429 carries `retry-after` and the student is told only "please wait". `clientIp()` also falls back to the literal `'unknown'`. **The identical bug was already found and fixed for `/super`, and the reasoning is written out in `lib/rate-limit.ts` lines 68-83; the fix never reached the payment route.**
 **D-1/D-2/D-4, one line, and my earlier note of "11 pages" was wrong.** `SiteFooter` is `async` solely for `await supportWhatsapp()`. Eleven pages import it; **only the three `'use client'` pages break, `/account`, `/practice`, `/universities`** which is why `/`, `/pricing` and `/c/[slug]` were fine all along. Two things I had not recorded before: **it is an endless loop** ("suspended by an uncached promise" repeats once a second and never stops, 57 in 46 seconds, still climbing; that is his *"glitching a lot a lot"* and the 480 errors), and **the footer's WhatsApp link silently becomes `/pricing`** on those pages while still reading "Message us on WhatsApp". Fix: sync `SiteFooterView` taking the href as a prop, plus a thin async server wrapper.
 **D-11, the QR upload can never work.** 400 KB data URL into `z.string().max(500)`. Workaround today: paste a hosted LINK. Confirmed live that the checkout shows no QR.
 **D-5, the staff passcodes are still `change-me-super` and `change-me-owner-different`.** Must be rotated before deploy.
 **NEW HIGH-SEVERITY, all found by running it:** **D-19** the payments queue can never ring anybody, the checkout asks for the LAST 4 DIGITS only and Google sign-in supplies no phone, so **there is nothing to ring, by design not by bug**; this makes **SA-7 the second rule marked PROVEN that is false**. **D-20** the API requires `confirmedInWalletLedger: true` so a human asserts they saw the money, and **the screen hardcodes it and never asks** while approving a consultancy, which moves no money, does show a confirm. **D-21** the dashboard headline reads **NPR 449 when NPR 25,449 has been taken** because consultancy revenue lives in a different overview. **D-18** creating and approving a consultancy write **no audit rows at all** though a consultancy changing its own passcode is audited carefully. **D-12/D-13** `/owner` says the platform is ON while it is OFF, and does not reload the saved contact number, so a second pause could ship an emergency screen with no phone on it. **D-14** Chrome autofills one portal's passcode into another's field.
 **WHAT IS GENUINELY GOOD, and should not be broken while fixing the above:** the audit trail contains **none** of the three passcodes; settings rows name each field that changed; the waiting screen names 4 hours, gives a reference to quote and an urgent human route; the queue says *"A screenshot is evidence, never proof"*; prices agree between `/pricing`, the checkout and the consultancy portal with nothing typed twice; refusals carry reasons and escapes nearly everywhere.
 **TWO TESTING TRAPS, written down so nobody chases them again.** (1) **`window.confirm` looks exactly like a frozen browser** to the Chrome tools, CDP times out, screenshots fail. `setConsultancyStatus` uses it. It is not a defect; it needs one human click. (2) **Always click "Show" before trusting a passcode field.** Chrome autofilled the super passcode into the consultancy box, and a dropped leading character produced "hange-me-super". Both cost me a false FAIL. I also formed a hypothesis that blocked calls extend the rate-limit block, **tested it, found it false, and did not record it** that is the standard.
 **NEXT, in the client's own order:** finish the remaining 74 rules (the interview room end to end, the results report, `/practice`, rejection and retry, the offer countdown, the NOT BUILT walk), **then fix every recorded defect** then the AI (he is buying Groq and Gemini access from NPR 7,000 and wants a running cost record), then deploy. **Four commits are still unpushed and `PUSH.command` still needs one double-click on his Mac.**

- **2026-08-14 (session 6, part 2, THE PASS IS COMPLETE. All 120 rules have a result):**
 **100 PASS, 7 FAIL, 2 PARTIAL, 3 BLOCKED, 8 on the X list. 30 defects open, 1 withdrawn. Nothing was fixed; the client's instruction was record-only and it was followed.** The sheet is `docs/TEST-RUN.md`.
 **THE ONE TO FIX FIRST, and it is not on the old list: D-27.** With no AI key set, the results report shows **"Almost ready", "75%", English clarity 75%, Real detail 88%, Genuine student 75%** generated from a canned paragraph the student never spoke. **Nothing on the report says the AI is off.** The interview room does say so, loudly and correctly; the report, which is the thing the student keeps and shows people, does not. This is not my standard: `lib/ai/stt.ts` states it above `sttIsMocked()`, *"Presenting invented text as a student's own answer is the exact failure we criticise the competitor for, and it is not excused by being a development convenience."* The server guard `"=== The guard. No transcript, no score. Ever. ==="` is real and correct, but it only fires when `stt.status !== 'ok'`, and the mock path returns `ok`, so demo text walks past a guard written for silence. **Fix before any student sees a trial on a keyless deploy, and before switching the AI on.**
 **D-28, on the same screen family:** the gate that asks for NPR 449 says *"You answered all 10 free questions"* after 2 were answered, and promises *"Buying a pack unlocks the remaining 7 of this same sitting"*, which **D-23** proves does not happen. Two false statements on the one screen where the student decides to pay.
 **D-26, small and time-critical:** `/super` says set `GROQ_API_KEY`; the interview room says add a **Deepgram** key. Groq is the primary in `lib/ai/stt.ts` and costs **$0.111/hour against Deepgram's $0.258**. He is buying today, tell him **Groq**.
 **THE PATTERN, now measurable: eleven instances of "the server is right and the screen never calls it."** D-15 (portal unenterable), D-19 (checkout never sends the five fields the API accepts, so the phone he wants to ring is permanently null), D-24 (admin AND student questions stored, never asked), D-29 (`revokedAt` read in six places, written nowhere, so a seat can never be taken back), D-31 (`allowlistedIps` read in the trial gate, written nowhere, so the consultancy-lab protection is dead code), plus the earlier ones. **`qa/reachable-check.js` catches none of them because the action always has a caller.** The single extra assertion that would catch most: **every settings field written must be read somewhere.**
 **WHAT IS GENUINELY SOLID, and must not be broken while fixing the above.** The money path end to end: pay, queue, approve, **credits on the very next request** `questionsAllowed` 10 to 17, approve twice grants once ("This payment was already approved. Nothing changed."), one transaction number unlocks one account (`TXN_ALREADY_USED`), one payment waiting at a time (`PAYMENT_ALREADY_WAITING`), opening the checkout twice reuses the order, abandoned orders expire. Tenant isolation with two real consultancies: Everest sees 1 student, Rival sees 0, and **no transcript, evaluation or score appears anywhere in the consultancy payload**. A consultancy can never approve its own seat purchase. An existing direct student following a consultancy link is **not** claimed and spends no seat. The owner kill switch, all six rules. The audit trail contains **none** of the three passcodes. The super passcode chain including the deploy-key recovery that was false when first written. The brute-force lock: 5 wrong guesses then 429 for 300s. `redact()` runs **before storage** not just before the model. Silence and the spend breaker are both checked **before** we pay to transcribe.
 **TWO THINGS I GOT WRONG AND CORRECTED, recorded so the next person trusts the sheet.** I wrote "11 pages break" for the async footer; **only 3 do** the `'use client'` ones. And I hypothesised that blocked calls extend the rate-limit block, **tested it, found it false, and left it out.** Also: `window.confirm` looks exactly like a frozen browser to the Chrome tools (`setConsultancyStatus` uses it), not a defect, needs one human click. And **always click "Show" before trusting a passcode field**; Chrome autofilled the super passcode into the consultancy box and cost me a false FAIL.
 **STILL BLOCKED, honestly: A-6 and SA-10** need a payment made by the consultancy's own student; Google auto-selected the first account on every re-sign-in so I could not get back into the second. The ownership check is proven from the negative side (A-7, A-8 both refuse), but the positive case is untested.
 **NEXT: fix, in this order.** D-27 and D-28 (honesty on the report and the paywall), D-17 (the checkout lockout), D-15 and D-16 (the consultancy channel), D-1/2/4 (the footer), D-26 (one word, before he buys), then the rest. **Then** the AI, then deploy. **Four commits are still unpushed and `PUSH.command` still needs one double-click on his Mac.**

- **2026-08-14 (session 6, part 3, FIX AND VERIFY LOOP. Client's method: fix one thing, immediately re-run the exact test that failed it, do not move on until it passes):**
  **SEVEN CRITICALS FIXED AND RE-PROVEN IN THE BROWSER. `tsc --noEmit` clean after every change.**
  **D-27, the worst one, is closed.** With no AI key the report used to print "Almost ready", "75%", "English clarity 75%" from a paragraph the student never said. Root cause found precisely: the answer route's guard, `"no transcript, no score, ever"`, only fires when `stt.status !== 'ok'`, and the mock provider returns `ok`, so demo text walked past a guard written for silence. Fixed by deriving demo-ness from the STORED TRANSCRIPT (`isDemoTranscript`, exported with `DEMO_TRANSCRIPT_MARKER`) rather than from the env, so a sitting done in demo mode still reports honestly after a real key is added. `buildSummary` now treats a demo-only sitting as not-heard: sub-scores null, no headline score, and the praise and next-steps rewritten so it never congratulates a student for words they did not say. **Re-tested live: "Practice mode / We were not listening yet, so this attempt has not been scored", three sub-scores "Not assessed", per-question badges "Sample answer, not scored", Behaviour still real at 76%.**
  **D-28.** The paywall said "You answered all 10 free questions" after 2. `TrialGate` was passed `questions.length`, the size of the sitting, not the answered count. Now reads **"You answered 1 of 10 free questions / You ended the interview early."**
  **D-25.** The room counted only answers given in THIS visit, so a resumed sitting said "0 done, 10 left". `alreadyAnswered` is now passed from the page, which already had it. **Re-tested: "Q 2/10, 1 done, 9 left" on re-entry.**
  **D-23.** The paywall promised "buying unlocks the remaining 7 of this same sitting" and nothing implemented it. Now done lazily in the session GET, and **deliberately including COMPLETED trial sittings**, because reaching the gate marks the sitting completed and the gate is exactly where the student is asked to pay, so a guard skipping completed sittings would have skipped the only case the promise describes. **Re-tested: 10 to 17 questions, isTrial false, status reopened, the answer and the resume point kept, no duplicates.**
  **D-17.** The limiter moved from the top of `/api/payment` to inside the `submit` branch only, so `create` and `status` are no longer charged; the waiting-screen poll went 8s to 60s; the key is now `pay:<studentId>:<ip>` so a lab does not share one bucket; and the 429 now names the real wait in minutes and says "if you have already paid, do not pay again". **Re-tested: 12 status polls and 15 creates, zero 429s, where the old code locked out after 10.** The support number is now also published on the public `GET /api/platform`, and the checkout renders a contact card under ANY error, so the "Talk to a person" card can never again be removed by the failure it exists for.
  **D-15.** The consultancy portal could not be entered by anyone. `login` is now allowed through the handover gate; everything else is still refused. The other half matters: while the code is temporary the login returns a HOLLOW payload (no students, no orders, no seats), so nothing about a student leaves the server until the shared secret is replaced. **I then caught my own regression** in the same loop: the portal rendered that hollow payload after the change, showing "Seats left 0" to a consultancy that had just bought four, so `changePasscode` now re-reads with the new code before showing the portal. **Re-tested twice with fresh consultancies, ending on Seats bought 6 / Seats left 6.**
  **D-16.** `overview`, `setConsultancyStatus` and `createConsultancy` all returned the whole Consultancy row, passcode included. One `withoutPasscode` choke point now. **Re-tested: the private code and all three handover codes appear nowhere in any response.**
  **D-1/D-2/D-4.** `SiteFooter` split into a sync `SiteFooterView` plus a thin async server wrapper; the three `'use client'` pages use the view and get the number from a new `useSupportNumber()` hook. **Re-tested: `/account` renders fully, `/practice` renders, and `/universities` produced ZERO console errors in 30 seconds where it previously produced one per second forever.**
  **D-26/D-3.** The interview room banner named a Deepgram key while `/super` named Groq, and Groq is the primary at less than half the price. The room no longer names a provider or `.env.local` at all: it says nothing is scored and nothing is a judgement of their English. **Tell him to buy GROQ.**
  **HONEST GAP, recorded as such: SA-15 / D-24 is FIXED BUT NOT PROVEN.** `extraQuestions` had two writers and no reader; `eligiblePool`, `getQuestion`, `buildQuestionPlan` and `buildPracticePlan` now read a merged pool primed by `primeExtraQuestions()` at every server entry point, and it typechecks. **I could not demonstrate an added question reaching a student in this session**: the plan is randomised, `create` reuses an open sitting by design, the session-create limiter cut the retries short, and the practice picker filters to known category labels. **Marked fixed-and-unproven, not passed.** Next session: complete the open sittings, then draw repeatedly until an `x-` id appears.
  **STILL OPEN, in priority order:** D-19/SA-7/SA-8 (the checkout never sends the five fields the API accepts, so the phone he wants to ring is permanently null), D-11/SA-16 (QR upload), D-22 (every out-of-range value says "Something went wrong"), D-31/S-73 (`allowlistedIps` unreachable), D-30/S-8 (a disabled student is told "please sign in first"), D-29 (a seat can never be revoked), D-21 (dashboard shows NPR 449 when NPR 25,449 was taken), D-20 (`confirmedInWalletLedger` hardcoded true), D-18 (consultancy create/approve write no audit rows), D-12/D-13 (owner status line and contact fields), D-7 (green "Not on" cards), D-9 (super admin session), D-14 (autofill across portals), D-5 (rotate the placeholder passcodes before deploy).
  **NOT YET RUN THIS SESSION: `qa/run-all.sh` and `next build`.** Do both before pushing. Four commits plus this work are unpushed; `PUSH.command` still needs one double-click.

- **2026-08-14 (session 6, part 4, D-19 DONE PROPERLY. The client's own priority, and his reasoning is the spec):**
  **His case, in his words:** roughly twenty payments a day once live; students will write "I have paid" and attach nothing, because the screenshot is optional and **must stay optional**; so the only way to settle a payment he cannot match is to ring them or open their WhatsApp. Without a number a student who genuinely paid waits hours, and somebody who did not pay slips through while he is dealing with the crowd. He is right, and it is a money control as much as a service one.
  **What was actually wrong:** the capture was BUILT and the screen never called it. `POST /api/payment submit` has always accepted `whatsappNumber`, `whatsappConfirmed`, `city`, `level` and `targetUniversity`, and the checkout sent none of them. That is why the queue said "not given" for every student and the directory had five permanently null columns.
  **The fix, and it makes the student's life easier not harder.** The checkout now asks for the FULL WhatsApp number instead of the last four digits, validated against `^(977)?9[678]\d{8}$` so it accepts a bare 10-digit Nepali mobile or the same number with the country code, and **required before "I have paid" enables**. The last four are DERIVED from it for the wallet match, so there is one field where there used to be two. `whatsappNumber` and `whatsappConfirmed: true` now go with the submit.
  **The queue is now built for twenty a day.** Each row shows the number as a `tel:` link and a green **WhatsApp** button that opens that student's own thread with the message already written, including the amount and the transaction number, so he can compare against their receipt without typing. "paid from ...5678" still sits underneath because that is what is checked against the wallet ledger.
  **Proven end to end in the browser:** paid NPR 799 as a student, then read it back from the queue (`payerPhone 9812345678`, `whatsappConfirmed true`, `payerPhoneSuffix 5678` derived) and from the directory. **SA-7 and SA-8 both move from FAIL to PASS.**
  **STILL OPEN, and I am not calling these done.** D-11/SA-16 the QR upload, D-22 every out-of-range value saying "Something went wrong", D-31/S-73 `allowlistedIps` unreachable, D-30/S-8 a disabled student told "please sign in first", D-29 a seat that can never be revoked, D-21 the dashboard showing NPR 449 when NPR 25,449 was taken, D-20 `confirmedInWalletLedger` hardcoded true, D-18 no audit rows for consultancy create/approve, D-12 and D-13 the owner status line and contact fields, D-7 green "Not on" cards, D-9 the super admin session, D-14 autofill across portals, D-5 rotate the placeholder passcodes before deploy. **And SA-15/D-24 remains FIXED BUT NOT PROVEN.**
  **NOT RUN SINCE THESE CHANGES: `qa/run-all.sh` and `next build`.** Both must pass before pushing.

- **2026-08-14 (session 6, part 5, ZERO FAILURES. Every defect fixed and re-tested):**
  **FINAL: 120 rules. 110 PASS, 0 FAIL, 0 PARTIAL, 2 BLOCKED, 8 on the not-built X list. 31 defects raised across the whole pass, 1 withdrawn as my own testing error, 30 FIXED.** `tsc --noEmit` clean. Static suites green: contract 25, reachable 6, copy 8, header 8, route 5.
  **Fixed in this final block, each re-tested against the running product:**
  **D-11 / SA-16, the QR, which he called MVP-critical.** The cap was `z.string().max(500)` against a 400 KB data URL, so it had never once worked and always answered "Something went wrong." Now accepts an https link OR an inline data URL, ceiling sized for the 400 KB the form already enforces, shape-validated. **A 193 KB image saved: `{"saved":true,"changed":["the QR image"]}`.**
  **D-22, which was HIDING D-11.** New `lib/zod-message.ts` names the field and the limit on every route. 99 approval hours now answers *"approval wait hours is too high. The most you can use is 72."* A generic error is not a small UX problem; it is a defect that conceals other defects.
  **SA-15 / D-24 PROVEN, not just fixed.** Added a question in a category that exists nowhere in the hardcoded bank, asked for a drill in that category, and got back `x-ec8103a5`, the question just added. `eligiblePool`, `getQuestion`, `buildQuestionPlan` and `buildPracticePlan` all read a merged pool primed at every server entry point. This also repairs the student-facing half: questions a student asks us to drill were in the same list and were never asked either.
  **D-21** total revenue now adds consultancy money to order money and shows the split: proven NPR 50,449 = 449 + 50,000. **D-18** create, approve, suspend and allowlist all write audit rows with seat counts and amounts: three rows proven. **D-20** approving a payment now asks "Have you found [txn] in the eSewa ledger for [amount]?" instead of the screen asserting it for you. **D-29** `revokeSeat` added through repo, API and a **Take seat back** button. **D-31 / S-73** `allowlistedIps` is a real field with a **Lab networks** button, so the 40-account lab threshold is reachable for the first time. **D-30 / S-8** a disabled student is told what actually happened and given a number, instead of a sign-in loop. **D-12, D-13** the owner screen reads real state and loads the saved contact details. **D-7** amber for attention, green only for good news. **D-9** the super passcode is held in `sessionStorage` for that tab. **D-14** `autoComplete="new-password"` plus a distinct `name` per portal.
  **THE GUARD CAUGHT ME, and that is the system working.** After adding `revokeSeat` and `setAllowlistedIps` I ran `qa/reachable-check.js` and it failed: *"2 server features that no person can reach."* Exactly the defect class this whole pass has been about, committed by me, caught within minutes. Both screens were then built and it is green again.
  **TWO THINGS CANNOT RUN IN THE SANDBOX AND MUST RUN ON HIS MAC.** The mount refuses to delete files under `.next`, so `next dev` dies with "Loading persistence directory failed" and `next build` with EPERM, the same restriction that forced `PUSH.command` to exist for git. **`VERIFY.command` is new, in the `Content Karkhana` folder: double-click it and it runs TypeScript, the whole gate and a real production build, then prints a plain answer.** Do that before pushing.
  **THE ONLY TWO OPEN RULES, and neither is a product defect.** A-6 and SA-10 both need a payment made by a consultancy's OWN student. Binding happens only when a NEW Google account signs up through the link; Chrome auto-selects the first account without showing the chooser, and the dev sign-in token is disabled whenever Firebase is configured. **Sixty seconds closes both: `/c/langtang-hub` in a private window, second Gmail, pay for any pack, approve it from the consultancy portal.**
  **STILL REQUIRED BEFORE LAUNCH: D-5.** `SUPER_ADMIN_PASSCODE=change-me-super` and `OWNER_ACCESS_KEY=change-me-owner-different` are still the setup placeholders. Rotate them in Netlify. The Change your passcode form is the route, and rotating the deploy key remains the way back in, which is proven.
  **NEXT: the AI.** Buy **GROQ**, not Deepgram: both screens now say so, Groq is the primary in `lib/ai/stt.ts` and costs $0.111 an hour against Deepgram's $0.258. D-27 is fixed, so switching it on is now safe; before the fix a keyless deploy showed students a fabricated 75%.

- **2026-08-14 (session 6, part 6, WHY NINE SUITES WENT RED, and it was ONE thing):**
  **The client ran `VERIFY.command`. TypeScript clean, all five static suites green, `next build` clean, and NINE server suites red: pilot, model, rules, journey, lifecycle, adversarial, fraud, tenant, backoffice, backoffice-ui, ai.** Almost every failure read `401`, `404` or `undefined`, and `rules-check` failed on **S-13, "the trial needs no card, no phone and no form"**, with `journey-check` failing on **"3. signs in with Google"**. That is not thirty broken features. That is one broken sign-in, and everything downstream of it.
  **ROOT CAUSE, and it is not any of the thirty fixes.** `lib/auth/firebase.ts` only ever reached its `dev:` token escape hatch when `firebaseWebConfig()` returned NOTHING, that is, only while Firebase was **unconfigured**. `.env.local` now carries `NEXT_PUBLIC_FIREBASE_API_KEY`, `AUTH_DOMAIN` and `PROJECT_ID`, which is exactly what we needed so real Google sign-in would work in the browser and which I used all session. The moment those keys existed, every `dev:` token from the harness was posted to Google, correctly rejected, and every suite that signs in died. **`git status` confirms I never touched `lib/auth/firebase.ts`, and `.env.local` is gitignored, so it changed outside version control.**
  **This is a nasty class of bug and worth naming: the test harness's ability to sign in depended on the product being MISCONFIGURED.** The suites were therefore guaranteed to stop working the day the product was set up properly, and the failure would look like thirty broken features rather than one broken key. That is why the 14 Aug log says "501 checks, all green" and today says nine red with nothing in between but configuration.
  **THE FIX.** The hatch is now explicit and independent of the Firebase config: never in production (which `next start` still proves), and otherwise only when the harness asks for it by name via `QA_ALLOW_DEV_TOKENS=1`. `qa/run-all.sh` now sets exactly that when it starts each server. An ordinary `next dev` on a laptop still behaves like production here, so nothing is loosened for real use. **`tsc` clean, reachable-check and header-check still green.**
  **RE-RUN `VERIFY.command`.** The nine suites should go green. If any suite is still red after this, it is a real defect and not the sign-in, and the output will finally be readable.
  **ALSO IN THIS BLOCK, from the client's questions about what happens after a student pays:**
  **The remaining 7 questions DO now appear.** That is D-23, fixed and proven earlier today: a paid student's half-finished trial sitting goes 10 to 17 questions, keeps its answers, keeps its place, and reopens. Before today it did not, while the paywall promised it did.
  **Practice survives when mocks run out, in both packs.** `canStartMock` and `canStartPractice` are independent in `lib/entitlement.ts`, so Prep at 3 mocks and 15 practice leaves all 15 practice questions usable after the last mock is gone, and Serious the same with 20. The messages are separate too: "You have used all the mock interviews in your pack" is distinct from the trial wording, and practice has its own sentence so a trial student is never told they have "used all" practice they never had.
  **What was genuinely MISSING, and is now built: the balance was nowhere except `/account`.** The client: "does it show it somewhere above, because it has to show it somewhere above." It did not. A student who never opened `/account` had no idea how many mocks were left until the moment they were refused one, and the "nearly out" nudge lived on the same page they were not visiting. **`SessionSnapshot` now carries `mocksLeft` and `practiceLeft`, `headerSession()` fills them, and the header shows a chip on every page: "3 mocks, 15 practice", turning AMBER at 1 or 0, reading "No mocks left, 15 practice" at zero, and linking to `/account`.** The client-side re-check reads the same numbers from `/api/me`, so the count stays right after an answer without a reload.
  **STILL TO CHECK when the suites are green:** the exact wording a student sees at 1 mock and at 0 in a real browser, and the `offerRenew` nudge on `/account` (it fires at `mocksLeft <= 2`).

- **2026-08-14 (session 6, part 7, THE HONEST READINESS ANSWER. Written because the weekly limit is close and the next person needs this, not a summary of good news):**
  **CAN HE MOVE TO THE AI? Buy the subscriptions now, do NOT wire it in yet.** Buying is the long-pole item and carries no risk: get **GROQ** for speech (not Deepgram, they cost $0.111 vs $0.258 an hour and both screens now say Groq) and **GEMINI** for the evaluator. But switching it on should wait for one green `VERIFY.command`, because thirty fixes went in today and the full regression has not passed since.
  **WOULD A DEPLOY TODAY BE CLEAN? No. Honest estimate: the core journeys are solid, the edges are not fully re-proven.** The split is exactly this, and it should not be blurred:
  **PROVEN IN A BROWSER OR AGAINST THE RUNNING SERVER (16):** the whole money path end to end; the report no longer inventing a score (D-27); the paywall count (D-28); the resume count (D-25); the remaining 7 questions unlocking (D-23); the checkout lockout gone (D-17); the consultancy portal being enterable at all (D-15); the passcode no longer leaking (D-16); `/account`, `/practice` and `/universities` rendering with zero console errors (D-1/2/4); the phone number reaching the payments queue and directory (D-19); the QR saving a 193 KB image (D-11); field-named validation messages (D-22); an added question being drawn and asked (SA-15); revenue totalling both channels (D-21); consultancy audit rows (D-18); the lab allowlist (D-31).
  **FIXED, TYPECHECKED, BUT NOT YET SEEN WORKING (8). This is where residual risk lives:** the disabled-student message (D-30), the owner status line and saved contact fields (D-12, D-13), the amber warning cards (D-7), the super admin tab session (D-9), the per-portal autofill names (D-14), the wallet-ledger confirm dialog (D-20, a `window.confirm` that automation cannot drive), the positive path of taking a seat back (D-29), the new header balance chip, **and most importantly the Firebase dev-token hatch fix, which is the one thing standing between him and a readable test run.**
  **SO THE ORDER IS: `VERIFY.command` green, then rotate the passcodes, then AI.** Not the other way round. Nine suites are the cheap way to convert those eight unverified fixes into certainty; a browser pass over them by hand is the expensive way.
  **D-5 IS STILL A HARD BLOCKER FOR ANY DEPLOY.** `SUPER_ADMIN_PASSCODE=change-me-super` and `OWNER_ACCESS_KEY=change-me-owner-different`. Anyone who guesses `change-me-super` holds every student record. Rotate both in Netlify before the site is public. The in-product Change your passcode form is the route, and rotating the deploy key remains the proven way back in.
  **WHAT IS GENUINELY DONE AND SHOULD NOT BE RE-OPENED:** 110 of 120 rules pass. The two that do not are A-6 and SA-10, both blocked by one thing, needing a payment from a consultancy's own student, closable in sixty seconds with a private window and the second Gmail. The eight X items are honest not-built entries, six of which need the AI keys or a real handset.
  **THE PATTERN THAT SHOULD SHAPE THE NEXT SESSION.** Eleven separate defects this pass were the same shape: the server was right and the screen never called it, or called it with half the payload, or sat behind a condition that could never be true. `qa/reachable-check.js` caught two of my own instances of it today, within minutes, which is why it should be run after every change rather than only at the end. **The single check that would catch most of the remainder: every settings field that is WRITTEN must be READ somewhere.** That is worth building before the AI work starts, because the AI adds two more settings and two more screens.

- **2026-08-14 (session 6, part 8, THE AI SUPPLIER DECISION, and it is NOT single-supplier):**
  **THE CLIENT'S CONSTRAINT, and he is right about it.** Groq's paid Developer tier shows *"Developer tier upgrades are temporarily unavailable due to high demand"*, so the cheap provider is one he **cannot pay**. His words, and they are the correct instinct: *"we really cannot trust the free trial"*, citing Google Flow quietly cutting 150 credits to 50 once people depended on it. A provider with no payable tier is a dependency you do not control.
  **THE DECISION: BOTH KEYS. Groq primary on its free tier, Deepgram as the paid safety net.**
  **AND THE ACTUAL GAP IS NOW CLOSED IN CODE.** `lib/ai/stt.ts` used to read "use Groq if the key exists, OTHERWISE Deepgram", with **no path between them**. A Groq outage, or simply hitting the free tier's 7,200 audio-seconds-per-hour ceiling on a busy evening, returned nothing to the student while a perfectly good Deepgram key sat unused in the environment. It now tries Groq and, on any `failed` result, falls back to Deepgram before giving up. **Silence and too-short answers are deliberately NOT treated as failures**, because retrying a quiet recording on a second paid provider only spends money to reach the same honest answer.
  **A CORRECTION I OWE THE RECORD.** I earlier told him the code's "$0.111/hour" comment was stale because the published turbo price is $0.04/hour. **I was wrong.** The code uses `whisper-large-v3` on purpose and explicitly rejects turbo, with the reason written in place: *"the saving is about four rupees per interview and it is less accurate. Mis-hearing a nervous student is not worth four rupees."* That is the right call for Nepali-accented English. Do not "optimise" it to turbo without benchmarking first.
  **FISH AUDIO WAS INVESTIGATED AND REJECTED, with numbers.** He asked because Chinese models are often cheaper, which is true of LLMs like DeepSeek but NOT true here. Fish Audio transcription is **$0.0002/second, which is $0.72/hour**: roughly 6.5x Groq and 2.8x Deepgram, the most expensive of the four checked. Their strength is text-to-speech and voice cloning; ASR is a side product tuned for Mandarin first, which is the wrong optimisation for Nepali-accented English.
  **PRICE TABLE, per full 17-question mock (about 12.75 minutes of audio):** Groq whisper-large-v3 $0.111/hr = **$0.024**; Deepgram Nova-3 batch about $0.26/hr = **$0.055**; OpenAI Whisper $0.36/hr = $0.076; Fish Audio $0.72/hr = $0.15. At 600 mocks a month: Groq about $14, Deepgram about $33, Fish about $92.
  **THE FREE HEADROOM HE ACTUALLY HAS.** Groq free tier: **2,000 transcription requests a day** (17 per mock, so about 117 full mocks a day) and **7,200 audio seconds an hour**. The hourly ceiling is the one that can bite: twenty students all sitting a full mock in the SAME hour is about four hours of audio and would throttle, which is exactly when the new Deepgram fallback earns its place. Deepgram: **$200 free credit, no card, credits never expire**, about 45,000 minutes, roughly **3,500 full mocks**, about six months at twenty students a day.
  **GEMINI, unchanged from part 7:** free tier for testing, but **switch to paid billing before real students**, because free-tier prompts may be used to improve Google's products and the transcripts contain students' family finances and visa history. That is a privacy commitment, not a cost decision.
  **WHERE TO GO, and what to check at each place, written out because he asked for it explicitly:**
  **GROQ, `console.groq.com`.** API Keys, create a key, that is `GROQ_API_KEY`. Then Settings, Limits to see the real per-model rate limits on his account rather than a blog's numbers. Then **Settings, Data Controls and Model Terms**, to find out whether the FREE tier permits Groq to use submitted audio; this matters because the audio is students talking about their family's finances. Settings, Usage is where the spend appears.
  **DEEPGRAM, `console.deepgram.com/signup`.** Sign up with email, Google or GitHub, no card. Then Settings, API Keys, Create New Key, that is `DEEPGRAM_API_KEY`. Check the $200 credit is showing under billing or usage before relying on it.
  **GEMINI, `aistudio.google.com`.** Get API key, that is `GEMINI_API_KEY`. For paid, enable billing on the Google Cloud project behind it.
  **WHERE THE KEYS GO.** `.env.local` for the machine he is testing on, and Netlify environment variables for the deployed site. **Never in the dashboard**: that is a locked decision from earlier, a key that can be typed into a web form can be read by anyone who reaches that form.
  **HOW HE VERIFIES IT WORKED, inside his own product.** `/super` dashboard: "Speech to text" and "Feedback" flip from "Not on" to "Live", the provider name appears, and "Paid calls this month" starts counting against the 6,000 ceiling. Those cards are now AMBER when something is off rather than green, which was D-7.
  **THE FIRST REAL TEST TO RUN, and it is the biggest open question in the business: X-3.** Sit one full interview himself and speak normally in his own accent. Every fairness rule (S-63 to S-69) is a mitigation for transcription being wrong, and `MIN_WORDS_PER_SECOND = 0.8` is a guess until measured against real Nepali-accented English. This is now SAFE to run, which it was not this morning: before D-27 a keyless or half-working setup showed students a fabricated 75%.

- **2026-08-14 (session 6, part 9, ALL THREE AI KEYS ARE IN AND PROVEN):**
  Keys set on his Mac via the new `SET-KEYS.command`, which prompts for each key with `read -s` and writes `.env.local` directly. **He never sends a key to the assistant and never types one into the app's dashboard**, which keeps the earlier security decision intact.
  **Proven by real API calls, not by assumption**, using the new `TEST-KEYS.command`: **Groq WORKS and `whisper-large-v3` is confirmed available on his free account. Deepgram WORKS.** Gemini failed 404 because `gemini-2.5-flash-lite`, the model hard-coded as the default in `lib/ai/evaluate.ts:88`, is **no longer available to accounts created after roughly April 2026**. His key was made 11 April 2026 and just missed it.
  **Fixed by `FIX-GEMINI.command`**, which lists the models his key can actually see, tries sensible candidates cheapest-first, and writes the winner to `GEMINI_MODEL` in `.env.local`. Result: **`gemini-3.5-flash-lite` WORKS and is now set.** `gemini-3.6-flash` also works as a spare. `gemini-3.7-flash` returned 503, likely load, and was rejected anyway on price grounds below. Note the code default in `evaluate.ts` is still the dead model, so **`GEMINI_MODEL` must be set in Netlify too**, or production will 404 while local works.
  **WHY NOT 3.7 FLASH, and it is the client's own reasoning applied again.** Gemini 3.7 Flash launched 13 August 2026 at $0.75 in and $3.75 out per million tokens, but that is an **introductory rate that doubles on 1 January 2027**. Building on a price with an expiry date is the same trap as the Google Flow credits going from 150 to 50. `gemini-3.5-flash-lite` at $0.30 in and $2.50 out is stable and works out around **half a rupee per mock**, which is negligible next to transcription.
  **Three new double-clickable files in the Content Karkhana folder**: `SET-KEYS.command`, `TEST-KEYS.command`, `FIX-GEMINI.command`. All three follow rule 0a: the client never touches a terminal command.
  **STILL OPEN:** restart the dev server and confirm `/super` shows Speech to text and Feedback as **Live**; then run **X-3**, one full interview in his own accent, which is still the largest unanswered question in the product. Also still open from part 8: re-run `VERIFY.command` after the Firebase dev-token fix, rotate the D-5 passcodes in Netlify, and close A-6 and SA-10.

- **2026-08-14 (session 6, part 10, X-3 SAT FOR REAL, AND IT FOUND THE WORST DEFECT IN THE PRODUCT):**
  **X-3 PASSED. Groq transcription of Nepali-accented English is good.** The client spoke a 45-second introduction and Whisper returned "Leeds Beckett University", "computational intelligence", "Coventry University", "Himalayan White House International College" correctly. Two flaws: it wrote **"Umangar Nirola"** for Umanga Niroula, and it appended text he never said. `/super` showed **Speech to text: Live (Groq Whisper large v3)** and **Feedback: Live (real marking)**. The 0.8 words-per-second threshold was not tripped.
  **D-35, AND IT IS THE MOST DANGEROUS DEFECT FOUND SO FAR.** He answered **1 of 10** questions, pressed End interview, and the report said **"You are close to ready for the real interview", 92%, green Ready badge**. The 92% was not wrong: it was the honest average of the one answer. **The VERDICT was wrong**, because a verdict needs coverage as well as quality. A student who sees "Ready" after one question can walk into a real credibility interview believing it. This is the same class as D-27 but survived it, because D-27 only guarded the case where we heard NOTHING. **Fixed in `lib/summary.ts`: the band is now capped by `completionRate`.** Above 90% answered the band is whatever the score earns. Between 60 and 90 percent, `ready` is demoted to `almost_ready`. **Below 60 percent no positive verdict is possible at all**, and the headline becomes "You answered N of M questions, which is not enough to judge whether you are ready". A first next-step now states plainly that the percentage covers only the answered questions. Quality can lower the band, never raise it above what coverage supports.
  **D-33: the most expensive button on the site was unguarded.** "End interview and see results" spent the entire mock on one click. After one question the universities page said "Buy a pack to start", the trial was gone, and nothing had warned him. **Fixed in `components/InterviewRoom.tsx`** with a real modal, deliberately NOT `window.confirm`, which renders as a frozen browser to the Chrome tools, cannot state how many questions remain, and looks like an error rather than a decision. It names the exact cost, shows answered against total, and the DEFAULT action is "Go back and finish the interview". Ending is still possible, labelled "Stop anyway and use up this mock". It does not appear when the last question is already answered, because that is not ending early.
  **D-36: Whisper was putting our own words in the student's mouth.** The transcript ended with "NEPALI STUDENT ASKING A QUESTION", which he never said, and it was displayed on the results page under "WHAT YOU SAID". **Root cause: it is our own `prompt` parameter in `lib/ai/stt.ts` line 133 bleeding into the output.** Whisper does this when audio ends in silence: with nothing left to transcribe it falls back on the conditioning text. **Fixed with `stripHallucinatedTail()`**, which only ever inspects the LAST sentence, and only drops it when it is short AND shouted in capitals, or when 60 percent of its words come from our own hint. Deliberately conservative: deleting something a student actually said is a worse failure than leaving an artefact. **Proven against five cases**, including two that must survive, "IELTS was 6.5 overall." and "I chose UEL.". Both survive.
  **STILL OPEN, both raised by the client and both agreed:** **D-34**, the results page is monochrome, so the band, the four sub-scores, the PEE chips and the behaviour table all read as the same green or white and weakness is invisible at a glance. He asked for a consistent scale, explicitly "not the Tihar festival". **D-32**, the `/super` Live cards need a red or green dot so an off provider is obvious. Also still open from earlier: the token cap cut from 900 to 420 with the numbered-step feedback rewrite, the researched per-question time limits (all 22 questions are currently 45 or 60 seconds, none at 90 or 120), the evidence method pack, and the split of Evidence into externally-checkable versus personally-specific.

- **2026-08-14 (session 6, part 11, D-37: THE WORST DEFECT IN THIS PRODUCT'S HISTORY, AND IT DESTROYED STUDENT DATA):**
  **The client sat a second interview and found it. Every symptom he reported was ONE line of code.** He answered questions 1, 2 and 3, and reported: the header stayed on "1 done, 9 left" forever, the new end-interview dialog always said "You still have 9 questions left" no matter which question he was on, and the final report contained **exactly one question card, showing question one's TEXT above question three's TRANSCRIPT**. He concluded, reasonably, that the counts were hardcoded. **They were not. Nothing was hardcoded.**
  **ROOT CAUSE, in `components/InterviewRoom.tsx`:** `rec.onstop = () => void upload()`. `startRecording` is memoised on `[startLiveTranscript]`, which never changes, so it is created **once, on question one**, and the `upload` closure it captured had **`question.id` frozen at `q-01` for the entire interview**. Every answer was therefore uploaded under question one's id. The server then behaved perfectly and made it worse: `answers.filter(a => a.questionId !== questionId)` correctly REPLACES any existing answer for that id, so answer two overwrote answer one and answer three overwrote answer two. At the end exactly one answer existed. **A paying student would have spoken for ten questions, been charged for ten, and been shown one, attributed to the wrong question.** Fixed with a `uploadRef` kept current by an effect, so the recorder is still created once but always calls the current closure. **This is the single most valuable thing found in six sessions of testing, and only a real sitting could have found it. `qa/reachable-check.js` cannot see stale closures.**
  **D-36 REOPENED AND WIDENED, because the first fix was too narrow.** The client's screenshot showed the leak surviving in a form I had not handled: "NEPALI student answering a UK university pre-CAS credibility interview about their course, university, tuition fees, sponsor, accommodation and career plans." That is **24 words**, and my first version capped BOTH detection paths at 10 words, so it caught only the short shouted variant. **Now split into two limits**: a shouted fragment stays capped at 10 words, because a long capitalised passage is more likely something the student really said, while a prompt echo is recognised by its WORDS not its length and is allowed up to 40. **Proven against six cases**, including the exact leak from his screenshot, and including a control that must survive, "My course is in accommodation and career plans at this university.", which uses prompt words in real speech. It survives.
  **STILL OPEN AND RAISED BY THE CLIENT THIS ROUND:** (a) **his own idea, and it is a good one**: when transcription fails, we currently make the student SPEAK AGAIN, which costs them an attempt and costs us nothing back. The audio is already held in `pendingRef` for the network-failure path. Offer **"try that recording again"** the way Wispr Flow does, so a transcription failure never costs a re-recording. (b) Question 4 failed transcription twice in a row while he spoke at the same volume as questions 1 to 3, and **the Deepgram fallback did not visibly fire**, because `silent` is deliberately excluded from retry. That exclusion may be wrong when the audio is clearly not silent. (c) Whisper appended the word "bus" when the 60-second limit cut him off mid-sentence. (d) D-34 colour coding and D-32 super admin dots, both still outstanding.

- **2026-08-14 (session 6, part 12, D-37 CONFIRMED DEAD, and D-39 found in the first complete sitting):**
  **D-37 IS FIXED AND PROVEN IN THE BROWSER.** The client sat all ten questions. Header reached **"10 done, 0 left"**, the end-interview dialog correctly said "5 of 10" when he tested it at question six, and the report contained **all ten questions with the right transcript under the right question**. Scores ranged 35 to 85 percent with a 57 percent overall and a "Needs practice" band, which is a believable spread rather than the single fabricated card of the previous sitting. D-35's coverage cap also behaved: 10 of 10 answered, so the band was allowed to be whatever the score earned.
  **D-39: WHISPER COMPLETES SENTENCES THE STUDENT DID NOT FINISH.** The client deliberately tested the most common real student failure: hesitating at the start, then being cut off by the timer. On question five he said *"One of the things I researched carefully is"* and the timer stopped him there. **Whisper returned "...is from the data that I found."** He never said those words. **His reasoning is the important part, and it is right: "if the AI starts filling in the gaps itself, then this mistake will never be caught."** A tidy invented ending does not just put words in a student's mouth, it **hides the very mistake the student needs to be told about**. Running out of time is a real and extremely common failure for nervous students, and it is invisible if the transcript always reads as a complete thought. Note he also observed the CORRECT behaviour on questions seven and eight, where the transcript honestly ended mid-phrase at "at University College," and he explicitly said that is what he wants.
  **WE CANNOT STOP WHISPER GUESSING, SO WE DETECT THE OPPORTUNITY INSTEAD.** We know how long the student spoke and how long they were given. `ranOutOfTime: durationSeconds >= question.timeLimitSeconds - 1` is now computed in the answer route and passed to the evaluator. One second of tolerance because the recorder stops a fraction after the limit. **Erring towards detecting it is the safer direction**: a false positive costs one extra line of timing advice, while a false negative means marking a student down for a sentence they were never allowed to finish, or praising them for an ending the machine invented.
  **THE PROMPT NOW HAS A RULE FOR IT.** When an answer was cut off: do not judge or quote the final words, **do not mark Wrap-up as present** because they did not reach it, and make the FIRST fix about time, specifically starting to speak as soon as the timer begins. It must also not read as an accusation of carelessness: running out of time usually means thinking too long before starting or giving too much background, and the feedback should name which one it can see.
  **STILL OPEN FROM THIS ROUND:** the client reports that **stopping the timer himself early may not transcribe what was recorded up to that point**. Unverified, and it needs a deliberate test: record thirty seconds of a sixty second question, press stop manually, and check the transcript arrives. Also still open: D-34 colour coding, D-32 super admin dots, D-38 retry-the-audio, the token cap cut to 420 with numbered-step feedback, the researched per-question time limits, and the evidence method pack.

- **2026-08-14 (session 6, part 13, D-40 INVESTIGATED, AND THE REAL COST MEASURED FROM THE CLIENT'S OWN CONSOLES):**
  **D-40, the client's suspicion, was WRONG, and checking it found a real defect beside it.** He suspected that stopping the timer himself loses the audio recorded up to that point. It does not. Both the countdown expiring and the stop button go through the same `stopRecording()`, which stops the recorder, fires `onstop`, and uploads. The paths are identical, so a manual stop transcribes exactly like a timed one. **His instinct that something was wrong in that area was right even though his diagnosis was not.**
  **THE REAL DEFECT: the answer a student was still speaking could be thrown away when they pressed "End interview".** `stopRecording()` does not upload synchronously; it only STARTS an upload via `onstop`. `closeSession` then carried straight on to `/complete` and navigated to the results page, so a student who was mid-answer raced their own recording. The summary was built, and often the page had already changed, before the last answer reached the server. **This is the quiet version of D-37**: nothing looks broken, an answer is simply missing, and only the student knows it should have been there. Fixed with `inFlightRef`, which `onstop` publishes and `closeSession` awaits. `.catch` swallows a failed upload deliberately, because a network failure must not trap the student on the interview screen when their earlier answers are already safe on the server and the results page rebuilds the summary from those.
  **REAL COST, MEASURED, NOT ESTIMATED. The client sent his Groq and Google AI Studio consoles after one complete ten-question sitting.** Groq: **14 API calls**. Gemini: **21 requests, about 20K input tokens and about 4K output tokens**, with 4 errors, all from the earlier `gemini-2.5-flash-lite` 404s and one 503 on `gemini-3.7-flash`. At `gemini-3.5-flash-lite` prices that is **$0.006 in and $0.010 out, roughly $0.016 or about NPR 2.2 per full mock**.
  **THAT IS WELL UNDER MY ESTIMATE, AND ONE OF MY EARLIER RECOMMENDATIONS IS NOW WRONG.** I predicted about NPR 7 per mock and proposed cutting `maxOutputTokens` from 900 to 420 to halve it. The measurement says **output averaged about 270 tokens per answer**, nowhere near the 900 cap, **so the cap is not binding and cutting it to 420 would save nothing**. The numbered-step feedback rewrite is still worth doing, but as a CLARITY change for the student, not as a cost control. Do not sell it to him as a saving.
  **WHAT THIS MEANS FOR HIS PRICING, which was his actual question.** At NPR 2.2 per mock for Gemini plus Groq free, the **NPR 499 pack of 3 mocks and 15 practices costs single-digit rupees in AI**, and even the NPR 799 pack of 10 mocks and 20 practices is comfortable. **The binding constraint is not money, it is the Groq free tier ceiling of 7,200 audio seconds per hour**, which is why the Deepgram fallback exists. He should watch the "Usage (24hrs)" column on the Groq key page and the `Paid calls this month` card on `/super`.

- **2026-08-14 (session 6, part 14, D-41 THE PAID UPGRADE RESTARTS THE PAPER, plus the em dash sweep and the Deepgram test):**
  **D-41, RAISED BY THE CLIENT AND NOT YET FIXED. This is the next thing to do.** He did the exact journey a real paying student will do: sat the free trial, answered questions, hit the gate, **paid, correctly received 3 mocks and 15 practices**, then pressed Continue. **The interview restarted at "Q 1/17, 0 done, 17 left"** instead of resuming at question 11 of 17. The universities page then said "You answered 1 of 17 questions". **His expectation is correct and it is the whole point of the trial design**: the free sitting is 10 of a 17 question paper, so paying should UNLOCK THE REMAINING 7 on the same paper, not hand him a fresh 17 with his earlier answers discarded. He had already asked about exactly this in an earlier session, and it was believed to work. It does not. **Investigate the upgrade path in the trial gate and whether it creates a NEW session rather than extending `questionIds` on the existing one.**
  **HE ALSO CONFIRMED WHAT DOES WORK, and it is worth keeping.** He clicked away to another university, got the leave warning, went back, and the "You have an interview in progress" banner was correct. He then **signed out and signed back in**, and the in-progress state survived. That is D-25 holding.
  **THE EM DASH SWEEP. MY FAULT, AND HE CAUGHT IT IN THE PRODUCT.** He has a standing rule of no em dashes and I let them into student-facing copy. He spotted "Nothing is lost , pick up exactly where you stopped." on the universities page and asked, correctly, whether it counts as a defect. **It does.** Rewritten as full stops or colons in: `universities/page.tsx` (the in-progress banner, both variants, and the search hint), `account/page.tsx` (the score-went-down message), `InterviewRoom.tsx` (the network-failure message), `answer/route.ts` (the too-quiet and too-short messages), and `FirebaseSignIn.tsx` (the error detail). **Deliberately left alone: the "," used as an empty-cell marker in the pricing table and the super admin orders table, which is table typography rather than prose.** Comments were not touched, only strings a student can read.
  **THE DEEPGRAM TEST, because he asked to judge it before trusting it as the fallback.** New `TEST-DEEPGRAM.command` in the Content Karkhana folder. It **parks `GROQ_API_KEY` as `GROQ_API_KEY_PARKED`**, which leaves Deepgram as the only transcriber, and running it again restores Groq. Nothing is deleted. This is necessary because the production order is Groq first with Deepgram only on failure, so **Deepgram would otherwise almost never run and could not be judged**. What he should compare: his own name, university names, technical words like computational and reinforcement and virtualisation, numbers like 9.1 million and September 2026, **and above all whether Deepgram INVENTS AN ENDING when the timer cuts him off, which is the D-39 behaviour Groq showed**.
  **DEPLOYMENT DECISION CHANGED, noted for the next session.** He no longer wants Netlify. He intends to **buy a domain and deploy properly** (he had Namecheap open). This changes the deploy checklist: `DEPLOY-TO-NETLIFY.command` becomes obsolete, and all four AI environment variables plus `GEMINI_MODEL=gemini-3.5-flash-lite` must be set on whatever host replaces it, along with the D-5 passcode rotation which is still a HARD BLOCKER.

- **2026-08-14 (session 6, part 15, DEEPGRAM JUDGED, HOSTING DECISION, THE REAL UNSUBSIDISED COST, AND A BIG UI DIRECTION CHANGE):**
  **DEEPGRAM WAS TESTED AND IT PASSES.** `/super` correctly showed **"Speech to text: Live, Deepgram Nova 3"**, proving the provider swap and the `/super` provider label both work. The client's verdict: *"it is doing phenomenal job"*. **A REAL DIFFERENCE WORTH KEEPING IN MIND: Deepgram preserved the hesitation markers, "uh" and "um", which Groq strips.** For an interview coaching product that is arguably an ADVANTAGE, because hesitation is diagnostic: a student who says "um" six times in an answer has a real problem the feedback should notice. It also did NOT invent an ending. Both providers still mis-hear the same two things: his name, and "Leeds Beckett" as "Leeds Package". **Conclusion: Deepgram is a genuine fallback, not a token one, and the D-39 invention problem may be Groq-specific.**
  **THE REAL COST WITH NO FREE TIER AT ALL, which he asked for directly.** Per full 17-question mock (about 12.75 minutes of audio): **Groq $0.0236 + Gemini $0.016 = about $0.040, which is NPR 5.6**. If Deepgram does the transcribing instead: **$0.055 + $0.016 = $0.071, about NPR 10**. Against his packs: the **NPR 499 pack costs about NPR 26 to serve** fully paid, and the **NPR 799 pack about NPR 68**. **His entire testing to date, four or five sittings, would have cost roughly NPR 25.** He is not close to a cost problem. The constraint remains the Groq free hourly ceiling, not money.
  **HOSTING: babal.host. HE MUST NOT BUY YET, HE MUST ASK FIVE QUESTIONS FIRST.** He is comparing their cPanel **Node.js plan at NPR 420/mo (3GB RAM, 2 vCore, NPR 15,120 for 3 years)** against their **VPS at NPR 1,260/mo (1 vCPU, 2GB, NPR 15,120 for 1 year)**. Same three-year outlay as one VPS year, so the Node plan is roughly a third of the cost. **The Node plan is probably enough for this MVP, but only if the answers are right**, because Next.js 16 App Router is NOT a static upload: it needs a long-running Node process. **The five questions: (1) which Node versions, Next 16 needs 20 or newer; (2) does the Node selector keep a persistent `next start` process alive, or does it idle out; (3) what is the HTTP request timeout, because `app/api/session/[id]/answer/route.ts` sets `maxDuration = 26` and a 10 or 15 second cap would break transcription; (4) can he set private environment variables, since all four API keys must live there and never in the dashboard; (5) does the filesystem persist between restarts, which matters for whatever `lib/store.ts` writes.** If they cannot answer 2 and 3 clearly, take the VPS. **Advice, not a recommendation: he has NPR 3,000 in the bank, and the NPR 10,000 difference is real money to him. Moving from shared hosting to a VPS later is a day of work, not a rewrite, so starting cheap is a reasonable risk.**
  **D-42, A REAL DEFECT HE REPORTED: `/super` SIGNS HIM OUT ON EVERY RELOAD.** His words: *"each time I reload, it's just logging me out... I don't want this session to be refreshed each time. I want there to be a logout button which logs me out."* Correct on both counts: the super session must survive a reload, and signing out must be a deliberate act. **Not yet investigated. Look at how the super passcode session is held and whether it is memory-only per server process rather than a cookie.**
  **TWO LARGE DIRECTION CHANGES HE WANTS, both recorded so the next session does not lose them:** (a) **A COMPLETE UI AND UX REDESIGN.** His words: *"I also wanna change the UI completely... it just won't look good."* This is a whole piece of work, not a tweak, and should be planned rather than started piecemeal. (b) **A STUDENT DASHBOARD.** Once a student signs in they should land on a **dashboard, like the admin and super admin portals have**, not on the marketing home page. His reasoning is sound: *"this home page is good to show to those who are visitors"*, but a signed-in student is not a visitor any more. This likely supersedes parts of the current `/universities` landing.

- **2026-08-16 (session 6, part 16, TESTING STOPS HERE. THE NEXT SESSION IS A REDESIGN, AND THIS BLOCK IS THE HANDOVER):**
  **THE CLIENT HAS DELIBERATELY STOPPED TESTING.** His words: *"from this point onwards, I am not testing anything because we are still going to dive into the UI UX change."* Do not ask him to sit another interview until the redesign is done. When testing resumes he wants it **deeper than last time**, because the pass we ran caught many defects but missed whole-journey ones like D-41 and D-42.
  **HIS DIAGNOSIS OF WHY WE MISSED THEM IS CORRECT AND SHOULD SHAPE THE NEXT TEST PLAN.** We tested SCREENS, not JOURNEYS. D-41 was invisible because there is no screen that shows "7 questions remaining"; he only found it by paying and pressing Continue. His exact point: *"if there was a dashboard, I could literally see that seven questions were remaining for me."* **The next test plan must be built from continuous journeys across sign-out and sign-in and payment, not from page checklists.**
  **DEEPGRAM IS NOW THE PREFERRED TRANSCRIBER ON QUALITY, NOT JUST AS A FALLBACK.** He tested it properly: *"nothing is leaking as well. Wherever I'm stopping, it is stopping as well."* **It does NOT invent endings, which means D-39 is a Groq-specific defect.** It preserves "uh" and "um", which he flagged as important for a reason worth keeping: **a student who fills every pause with "uh" must be TOLD so**, and a transcriber that silently deletes hesitation hides a real interview problem. **Consider making Deepgram primary once the $200 credit economics are understood.**
  **D-43, RAISED THIS ROUND: "Answer again" costs a paid call and nothing warns anyone.** His point: every retry re-transcribes and re-marks. Combined with D-38 (retry the stored audio rather than making the student re-speak), this is the whole retry story and should be designed once, not patched twice.
  **D-44, RAISED THIS ROUND: the credit chip showed "2 mocks left" before he had sat any exam.** His words: *"I've not even given one exam, and it shows two mocks left."* Needs checking against the ledger: either the chip is wrong or a credit was consumed without a sitting.
  **THE FULL BUILD LIST HE WANTS BEFORE DEPLOYMENT, in his order:**
  **(1) COMPLETE UI AND UX REDESIGN.** *"This UI is not working with me at all."* He wants **minimalistic, plain, easy, Apple-like**. He suggested looking at published UI skills, naming a GitHub repository he called **Genjutsu**, and asked that other design skills be considered too. **Find and evaluate real design skills before inventing a house style.**
  **(2) A STUDENT DASHBOARD, and a sign-in that routes to it.** On Google sign-in, **check whether the student has paid**. Paid students land on a dashboard; unpaid students keep the current journey. **Crucially, a paid student who exhausts everything STAYS on the dashboard** rather than being dropped back to the marketing home page, and the dashboard is where the buy-again prompt lives. The dashboard is also where "7 questions remaining" would have been visible, which is how D-41 would have been caught.
  **(3) REBUILD THE UNIVERSITIES PAGE.** *"I don't want the university page to be looking something like this."* Easier navigation. **Also still outstanding: download and use the real university logos.**
  **(4) MOBILE IS NOT SECONDARY.** *"The priority should not only be given to the website. The priority should also be given to the phone version."* Every redesign decision must be tested on a phone, and the next test plan must run on a phone as well as a laptop.
  **(5) A NEW AND DEEPER TEST PLAN**, journey-based as above.
  **(6) THE INVESTOR COSTING DOCUMENT: DONE THIS SESSION**, at `docs/UNIT-ECONOMICS-FOR-INVESTORS.md`.
  **THE COSTING DOCUMENT, AND THE ERROR HE CAUGHT IN HIS OWN PROPOSAL.** He asked for **inflated planning costs rather than measured ones**, and his reasoning is sound and should be respected: measured cost is NPR 5.60 per mock, but it comes from a handful of test sittings, not from hundreds of real students, and a founder who plans against NPR 5.60 has no room if a provider changes its price. He proposed NPR 50 per mock for the 499 pack and **NPR 100 per mock for the 799 pack, then asked "Am I correct?" He was right to ask, because NPR 100 breaks it.** At NPR 100 per mock the 799 pack costs **NPR 1,060 to serve against NPR 799 of revenue, a loss of NPR 261 on every sale**. **NPR 50 per mock is the highest planning figure BOTH packs survive**, and the document uses it throughout. **The document also states the structural finding plainly: the 799 pack is the THINNER product, keeping NPR 239 against the 499 pack's NPR 304**, because it costs almost three times as much to serve while earning only 60% more. Two fixes are offered and neither is urgent: cut it from 10 mocks to 7, or raise it to NPR 999.
  **ALSO IN THE DOCUMENT, because he asked for all of it:** cost per answer by provider, both packs side by side at measured and planning cost, fixed monthly costs, **the survival number of TWO paying students a month**, a students-to-profit table up to his 600 a month target, **what to load on the dollar card at each volume ($15, $40, $150)**, the fact that the real constraint is the Groq hourly ceiling rather than money, and an honest risks section covering the closed Groq Developer tier and the Gemini 3.7 introductory price expiring on 1 January 2027.
  **STILL OPEN AND UNFIXED GOING INTO THE REDESIGN:** D-32 super admin status dots, D-34 results page colour coding, D-38 retry the stored audio, D-41 paying restarts the paper instead of unlocking the remaining 7, D-42 `/super` logs out on reload and needs a real logout button, D-43 "Answer again" costs a paid call silently, D-44 the credit chip read "2 mocks left" before any sitting. **Plus the still-unrotated D-5 passcodes, which remain a HARD BLOCKER before anything is public.**

- **2026-08-16 (session 6, part 17, THE COSTING DOC REWRITTEN AT 3x, AND THE INVESTOR DEFENCE):**
  **I OVERCOMPLICATED THE FIRST VERSION AND HE TOLD ME SO, PLAINLY AND CORRECTLY.** *"You're confusing me so much."* The first draft had planning costs, measured costs, multipliers, survival tables and risk sections all at once. **Rewritten as one short document with four tables and no jargon.** If it is edited again, keep it that short.
  **HE CHOSE 3x, NOT 8x.** Real cost NPR 0.35 per question and NPR 6 per mock, presented at 3x: **NPR 1 per question, NPR 18 per mock, NPR 54 for three mocks, NPR 180 for ten.** Packages: **499 pack costs NPR 69 and keeps NPR 430. 799 pack costs NPR 201 and keeps NPR 598.** Both healthy at 3x. Note the earlier finding still holds underneath: the 799 pack is proportionally thinner, but at 3x that does not show as a problem.
  **THE QUESTION THAT MATTERED MOST, AND HIS INSTINCT WAS EXACTLY RIGHT.** He asked: what happens when an investor Googles the published Groq, Deepgram and Gemini prices and sees they are lower? **He explicitly rejected a weak answer** (*"I don't know about this, let me check"*) and said the answer must be **about how many tokens we send and receive**. That is correct, and it is also true, so the document does not have to fabricate anything.
  **THE DEFENCE, all of it verifiable in our own code:** (1) **We send about 1,900 words per question**, not just the answer: a 1,000-word marking instruction, the question and its private rubric notes, the student's last three answers for contradiction checking, and the answer itself. **The instruction alone is sent 17 times per mock and is the single largest cost in the product.** (2) **We deliberately use `whisper-large-v3` and NOT the turbo version at a third of the price**, because turbo mishears Nepali-accented English. (3) **We pay a second supplier, Deepgram, to stand behind the first at about 2.5x the cost.** (4) **The figures are built on the NERVOUS student**, who speaks the full 60 seconds and retries, not the confident one. (5) **Three retries per question are allowed, each fully re-transcribed and re-marked.**
  **THE DOCUMENT ALSO LISTS THE SETTINGS AND WHY**, which is the part an investor can check: whisper-large-v3, Deepgram Nova 3 as automatic backup, Gemini 3.5 Flash Lite chosen for stable pricing with no expiry, `MAX_TRANSCRIPT_WORDS` 400, `maxOutputTokens` 900, three retries. **The last two are described as HARD LIMITS, which is the strongest line in the document: no single student, however they behave, can cost more than the stated figure.**
  **Closes with the one number he should lead with: two paying students a month covers the entire running cost.**

- **2026-08-16 (session 7, part 1, THE REDESIGN SPEC AND THE DESIGN SYSTEM. Read REDESIGN.md before touching any UI):**
  **`REDESIGN.md` is the new governing file for everything a person SEES AND CAN REACH.** `RULES.md` still governs behaviour. The client asked for one file containing every change plus better rules than last time, because *"some of the rules that we have were a bit ambiguous."*
  **HIS CRITICISM IS CORRECT AND THE SPEC OPENS BY MEASURING IT.** `RULES.md` has **100 rules marked BUILT + PROVEN behind 501 assertions, and every defect he found by hand got past all of it.** Not because the tests were weak — because of the WORDING. *"The header shows who is signed in"* is ambiguous in the one way that matters: **it does not say WHEN.** A test can prove the header renders a name while the product shows "Sign in" to a signed-in student for four seconds on every load, and both statements are true at once.
  **THE RULE FOR WRITING RULES, FROM HERE ON: a rule must name the thing that would make it false.** If you cannot finish *"this rule is broken when..."* with something observable, the rule is not written yet. Every rule in `REDESIGN.md` carries its own falsifier.
  **THE FIVE DEFECT SHAPES, and every bug of 13–16 Aug is one of them.** Learn the shapes, not the instances: **F-1** a conclusion rendered where a state belongs (the header; "you have used your free questions" while resumable). **F-2** a number written down instead of derived (`currentIndex` vs answers → "Q 8/10 · 1 done, 9 left"; 12 mocks promised where 10 were granted; "From NPR 449" after the price change). **F-3** a control whose destination was never checked ("Choose Prep" → `/start` → `/universities`; Skip telling nobody). **F-4** a server feature with no door (`setPaymentSettings`, `payerPhone`, and ten more). **F-5** proof of the code mistaken for proof of the product (nine green suites on a stale build; four tests matching comments).
  **THE TOKENS ARE IN: `app/globals.css` + `tailwind.config.ts`.** One home for every colour, duration, easing, radius and type size. Grounded in the genjutsu principles the client named — **duration by context (120 tap / 240 panel / 320 route, nothing past 500ms), easing by DIRECTION, exit always subtler than enter, `transform` and `opacity` only, and reduced motion delivering the FINISHED state rather than a frozen one.** That last point is the one usually got wrong: setting duration to zero leaves an element that animates IN parked at opacity 0, which is worse than the animation.
  **Fixed while enforcing it:** 12 raw `#eff4ff` tints across six files → `--surface-sunk` (the value was not wrong, it was **homeless**, which is F-2 waiting for the first person to adjust one of the six); the default consultancy brand hex written **four times in one file** and duplicating `--ink` → `lib/branding.ts` once (it must be a literal somewhere because `<input type="color">` cannot read a CSS variable); and **20 places using 10px and 11px text.**
  **A JUDGEMENT CALL WORTH KNOWING ABOUT.** The first version of D-8 said "nothing below 15px" and failed instantly on those twenty. **The resolution was NOT to soften the rule to fit the code**, and not to force a 15px status chip that no longer fits its badge. It was to separate **reading from glancing** — 16px prose, 15px labels, 13px glanceable chrome via `text-micro` — and ban everything below 13px outright. The spec records the amendment **and the reason**, so nobody later reads it as laziness.
  **`qa/design-check.js`: 18/18, verified against TEN mutations** (raw hex, >500ms, keyframe animating height, `scale(0)`, frozen reduced motion, removed focus ring, sub-16px input, 10px text, a token pair failing WCAG AA, a hover-only control).
  **AND THE MOST USEFUL THING THE MUTATION RUN PRODUCED, which is F-5 again:** M1 **passed when it should have failed**, because the mutation never applied — it targeted a class name absent from the file it edited. The check was fine; the mutation was a no-op and the green result meant nothing. **A mutation test that does not verify the mutation applied proves nothing.** Every mutation from here asserts the file actually changed before reading the result. Recorded in `REDESIGN.md` Part 8.
  **NEXT, in the spec's own order (Part 6):** the dashboard + sign-in routing, **which carries D-41** — and these are ONE job, because D-41 hid precisely for the reason the client identified: there is no screen showing "7 questions remaining", so fixing it without building that surface just hides it again. Then the universities page (U-1..U-3, including real logos), then D-42/D-44/D-38+D-43/D-32/D-34, then `dashboard-check.js`, then the journey suite (T-1, T-2), then deploy verification (T-4), then the new GitHub + Netlify + Firebase domain + env vars, then the phone pass (T-5).

- **2026-08-16 (session 7, part 2, THE STITCH REDESIGN LANDS. Design only — the lifecycle was not touched):**
  **26 Stitch screens + its `DESIGN.md` are kept at `docs/design-reference/stitch/`** (PNG + HTML for each). Its system matched the tokens already in the repo almost exactly — same navy/paper/GO, same serif+grotesk pairing, same 12/16 radii, same soft shadow — so nothing had to be renegotiated. **Read that folder before changing any screen.**
  **`components/ui/index.tsx` IS NOW THE ONLY WAY TO BUILD A SCREEN.** Page, Card, Button, ButtonLink, Chip, Eyebrow, Banner, StatRing, ProgressBar, Monogram, Row, EmptyState, BandChip, icons. **This is D-1 with teeth:** `design-check` can stop a raw hex, it cannot stop nine pages each inventing a slightly different card. The kit can. **It holds no state, fetches nothing, decides nothing** — if a component in it ever needs to know whether a student has paid, it is the wrong component.
  **`/dashboard` is new and it CARRIES D-41.** The client's own diagnosis was that D-41 hid because no screen showed "7 questions remaining"; the balance rings are therefore first and largest, and the resume banner states `answered of total` as a number on a page students actually visit. **It reads `/api/me` and `/api/account` and adds no endpoint, no entitlement logic and no state machine.** The ring denominator is derived from the ledger — **`copy-check` caught me typing "17 questions" by hand within a minute of writing the file**, which is the same F-2 that put "12 mocks a seat" on `/consultancy` for a week.
  **`/results` is colour-coded as the client asked.** The band card takes the colour of the result; a four-zone gauge makes a score a POSITION rather than a mark out of a hundred; each sub-score carries its own band on its left edge so a student sees WHICH part let them down. **A null renders an em dash and "Not assessed — we could not hear enough to judge this", never a zero** (G-1). Colour never travels alone.
  **`/universities` rebuilt** — search first and large, filter pills, ONE action per card whose label never promises what the server will refuse, and the shared `Monogram` tile so a missing logo cannot shift the layout.
  **THE MISTAKE, AND IT IS THE MOST IMPORTANT THING IN THIS ENTRY.** Converting the palette I replaced the **prefix** `bg-slate-` with `bg-surface-sunk`, which silently turned `bg-slate-300` into **`bg-surface-sunk300`** — a class Tailwind does not generate, so it emits **nothing**. **Eighteen of them.** A disabled button lost its background; the recording indicator stopped being red; a modal scrim disappeared. **`tsc` green. Every suite green. `next build` green.** Nothing could see it, because **a class that does not exist looks exactly like a class you never wrote.** All eighteen repaired to what the original shade meant, and **`design-check` D-1c now fails the build on any token name with a digit glued to it** — verified by reintroducing the exact damage, **with an assert that the mutation applied first**, because a mutation that silently no-ops proves nothing (the M1 lesson from part 1).
  **Green:** design 19/19, copy, route, header, reachable, `tsc`, and `next build` compiles + generates all 19 pages. (The `EPERM ... .next/export-detail.json` at the very end of `next build` is the **mount** refusing a `.next` unlink, not a code error — the build itself has already succeeded by that point.)
  **STILL TO CONVERT ONTO THE KIT:** `/` home, `/pricing`, `/checkout`, `/start`, `/account`, `/practice`, the interview room, `/admin`, `/super`, `/owner`, `/consultancy`, legal pages. They are all **palette-correct** (the sweep reached every file) but still use their old layout rather than `Page`/`Card`/`Button`. **Nothing is broken there — it is unfinished, not damaged.**
  **STILL MISSING: the 12 images.** `docs/IMAGE-PROMPTS.md` has the prompts and the exact filenames; they belong in `public/img/`. The dashboard empty state already references `/img/empty-reports.png` and degrades safely if it is absent.
